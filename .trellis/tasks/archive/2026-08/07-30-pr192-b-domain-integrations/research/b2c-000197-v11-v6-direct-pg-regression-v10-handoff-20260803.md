# v10 sealed candidate

v10 is self-contained and does not import or delegate to v7. It performs no command unless all DB, QA, drain and resource authority files pass immutable SHA, mode and schema checks. v7 remains an immutable non-production audit artifact.
