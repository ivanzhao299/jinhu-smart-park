# Real exception review and envelope preparation

## Goal
Close the missing operational preparation path between validated T0-T3 candidates and the existing freeze/execute consumers. Preserve the full HR migration goal; this task is not production activation or completed migration.

## Confirmed facts
- Merged current C is 8b325e2b9daac45b6e24d9f9f5f98da8cec8475a. Main CI and production deployment succeeded.
- Real candidates contain 260781 inserts and 47 quarantines. Current freeze completed REVIEW_HOLD and has no reviewed decision wrappers.
- The execution CLI already authenticates retained AES-GCM envelopes against exact payload/AAD before database access.
- Production code has no complete non-test producer for exception envelopes and externally signed candidate resolutions in the inspected path.
- Production target allowlist is registered; execution activation remains HOLD. Three independent approval subjects are still required by existing final execution contract.

## Requirements
- Provide an actual bounded private-file command, not only an unused helper, to prepare quarantine envelope/signing inputs and finalize externally supplied signed resolutions.
- Reuse existing candidate validation, field normalization, canonical payload hashes, crypto primitive, review schema and freeze consumer; no second generator or writer.
- Inputs must explicitly bind candidate/phase/inventory/scope/triple and reviewed partial target fields/dependency choices. Never infer missing parents, change original reasons or turn quarantine into insert.
- Encrypt exact normalized payload once using explicitly supplied external key material at the private IO boundary; never discover/generate production keys or sign as an invented authority.
- Finalization accepts external attestations and an explicitly pinned external reviewer public-key allowlist; verify real signatures and exact binding, not just public-key headers. No signer identity or organization authority is inferred from a self-supplied key.
- Preserve original controlled sources and exception evidence. Empty target fields cannot be silently treated as full source archival.
- No DB/source extraction, activation, payroll, binary import, secret/value/path output, overwrite or cleanup.

## Acceptance
- A synthetic nonempty quarantine flows through private prepare -> genuine external test signature -> finalize -> existing freeze and actual execution crypto verifier with matching plaintext and unchanged envelope bytes.
- Wrong key/tag/payload/operation/scope/triple/source/candidate/inventory/decision/signer and missing/extra/duplicate reviews fail closed; unsigned preparation cannot be fed as reviewed evidence.
- Private IO permissions, no-follow, explicit hashes, finite bounds, exclusive outputs, receipt-last and safe errors tested.
- Current HOLD and three-role authorization requirements remain unchanged. Existing freeze, bridge, generator and crypto tests pass.
- CLI directions clearly separate synthetic verification, real preparation, external review authority and actual production readiness.

## Authorization and open questions
User has explicitly authorized continued implementation without repeated process approval. No product choice blocks this preparation connection. Actual trusted reviewer/key artifacts and production execution approval are external evidence, not generated by this task.
