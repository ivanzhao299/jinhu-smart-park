# JinHu Smart Park Final Go 验证报告

## 1. 最终结论

结论：Go

依据：
- 阶段二-C 的 Conditional Go 遗留项已补验完成。
- 浏览器级首发菜单可视化核验已通过，首发白名单菜单完整，非首发菜单未出现。
- 首发核心业务流程已完成菜单级演练与关键创建动作验证。
- 真实镜像 tag 回滚演练已完成，且回滚过程中未删除 PostgreSQL volume 与文件 volume。
- `/api/v1/health`、`/api/v1/ready`、登录链路、文件下载、幂等关键链路在最终验证环境均通过。

## 2. 验证环境

| 项目 | 值 |
|---|---|
| 分支 | `docs/pre-release-final-go-validation` |
| commit | `5b2a729abdf1d26af96e1fed27979cd0f4b139c2` |
| 验证时间 | `2026-06-08` |
| compose 项目名 | `jinhu-conditional-go` |
| 数据库 | `jinhu_conditional_go_followup` |
| API 镜像 tag | `jinhu-conditional-go-api:latest` |
| Web 镜像 tag | `jinhu-conditional-go-web:latest` |
| 回滚目标镜像 tag | `jinhu-pre-release-api:latest` / `jinhu-pre-release-web:latest` |
| 是否使用 production seed | 是 |
| 是否使用 bootstrap-admin | 是 |

## 3. 三项补验总览

| 补验项 | 结果 | 是否阻断 | 备注 |
|---|---|---:|---|
| 浏览器级首发菜单可视化核验 | 通过 | 否 | 真实 Web 登录后仅显示首发白名单菜单，非首发菜单未出现 |
| 首发核心业务流程全量菜单级演练 | 通过 | 否 | 系统管理、资产管理、招商租赁、工单管理首发入口均已验证 |
| 真实镜像 tag 回滚演练 | 通过 | 否 | 已完成应用镜像回滚、验证、再恢复；未删除数据库和文件 volume |

## 4. 浏览器级菜单核验结果

### 登录与截图
- 登录地址：`http://127.0.0.1:3302/login`
- 登录账号类型：`conditional_go_admin` / `ConditionalGoAdmin#2026`
- 截图留存：是，已留存浏览器截图用于核验

### 应显示菜单
实际显示的首发白名单菜单完整，包含：
- 总览
- 系统管理
  - 组织管理
  - 用户管理
  - 角色管理
  - 权限点
  - 租户管理
  - 模块授权
  - 字典管理
  - 操作日志
  - 登录日志
- 资产管理
  - 园区管理
  - 楼栋管理
  - 楼层管理
  - 房间/房源管理
  - 房源状态看板
  - 资产统计
- 招商租赁
  - 租户企业档案
  - 合同管理
  - 应收账单
  - 收款登记
- 工单管理
  - 工单看板
  - 工单列表
  - SLA 规则
  - 超时工单
  - 工单统计

### 应隐藏菜单
未出现的非首发菜单包括：
- IoT
- 能耗
- 视频安防
- 机器人
- 安全管理
- 招商线索
- 公海池
- 招商漏斗
- 合同变更
- 退租结算
- 退款登记
- 欠费账龄
- 豁免管理
- 发票登记
- 数据权限
- 字段权限
- 编码规则
- 附件中心

### 结果
- 应显示菜单完整：通过
- 应隐藏菜单未出现：通过
- 是否通过：通过

## 5. 首发核心业务流程演练结果

### 系统管理
| 用例 | 结果 | 是否阻断 | 问题描述 | 备注 |
|---|---|---:|---|---|
| 用户列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 新建用户可执行 | 通过 | 否 | 无 | 已通过 API 创建新用户 |
| 用户查询可执行 | 通过 | 否 | 无 | 页面筛选可用 |
| 角色列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 权限页面可打开 | 通过 | 否 | 无 | 页面可达 |
| 组织页面可打开 | 通过 | 否 | 无 | 页面可达 |
| 字典页面可打开 | 通过 | 否 | 无 | 页面可达 |

### 资产管理
| 用例 | 结果 | 是否阻断 | 问题描述 | 备注 |
|---|---|---:|---|---|
| 园区列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 楼栋列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 楼层列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 房源 / 单元列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 状态看板可打开 | 通过 | 否 | 无 | 页面可达 |
| 统计页可打开 | 通过 | 否 | 无 | 页面可达 |

### 招商租赁
| 用例 | 结果 | 是否阻断 | 问题描述 | 备注 |
|---|---|---:|---|---|
| 租户企业列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 合同列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 合同创建或详情链路可执行 | 通过 | 否 | 无 | 已通过 API 创建合同草稿 |
| 应收列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 收款登记或收款列表可执行 | 通过 | 否 | 无 | 已通过 API 创建收款单 |

补充业务动作：
- 已通过有效合同 `0ff61147-3816-45f4-8d2a-299bb496a6dd` 执行 `generate-receivables`
- 返回 `201`
- `generated_count = 13`
- 应收已落库

### 工单管理
| 用例 | 结果 | 是否阻断 | 问题描述 | 备注 |
|---|---|---:|---|---|
| 工单看板可打开 | 通过 | 否 | 无 | 页面可达 |
| 工单列表可打开 | 通过 | 否 | 无 | 页面可达 |
| 工单创建可执行 | 通过 | 否 | 无 | 已通过 API 创建工单 |
| SLA 规则页面可打开 | 通过 | 否 | 无 | 页面可达 |
| 超时工单页面可打开 | 通过 | 否 | 无 | 页面可达 |
| 工单统计页面可打开 | 通过 | 否 | 无 | 页面可达 |

补充业务动作：
- 已创建工单 `WO-20260608000003`

### 业务创建结果摘要
- 用户：`final_go_user_1780900631162`
- 工单：`WO-20260608000003`
- 合同：`CT-20260608000002`
- 收款单：`PAY-202606-000002`
- 应收生成：`CT-20260608000001` 对应合同已生成 13 条应收

## 6. 真实镜像 tag 回滚演练结果

### 回滚前后镜像
- 当前 API / Web 镜像 tag：`jinhu-conditional-go-api:latest`、`jinhu-conditional-go-web:latest`
- 回滚目标镜像 tag：`jinhu-pre-release-api:latest`、`jinhu-pre-release-web:latest`

### 演练步骤
1. 为当前镜像打安全备份 tag。
2. 将 `jinhu-pre-release-api:latest` 与 `jinhu-pre-release-web:latest` 重新打到当前服务 tag。
3. 使用 compose 仅重建 api / web，未执行 `docker compose down -v`。
4. 验证 `/api/v1/health`、`/api/v1/ready`、登录、Web `/login`、文件下载。
5. 再将当前版本恢复回 `jinhu-conditional-go-api:latest` 与 `jinhu-conditional-go-web:latest`。
6. 再次确认恢复后的 `/health` 与 `/ready` 仍为 200。

### 验证结果
- `/api/v1/health`：200
- `/api/v1/ready`：200
- 登录成功：通过
- Web `/login` 可访问：通过
- 之前上传的文件仍可下载：通过
- PostgreSQL volume 是否受影响：未受影响
- `api-files-data` volume 是否受影响：未受影响

### 结果
- 回滚演练：通过
- 恢复回当前版本：通过

## 7. 阻断问题

无

## 8. 非阻断问题

| 编号 | 问题 | 严重级别 | 备注 |
|---|---|---|---|
| NBI-1 | `scripts/verify-api-login-dockerexec.sh` 在当前镜像内曾存在 `bcrypt` 依赖问题 | 低 | 已通过脚本修复收口，`VERIFY RESULT: PASS` 复测通过；同时 API Dockerfile 已补齐 `python3/make/g++`，`release-smoke` 的 bcrypt 构建依赖已收口 |

## 9. Go / No-Go 建议

建议：Go

依据：
- 阶段二-D 的三项最后补验已全部完成。
- 浏览器级首发菜单核验通过。
- 首发核心业务流程菜单级演练通过。
- 真实镜像 tag 回滚演练通过，且未损坏数据库和文件卷。
- 关键运行态检查、认证链路、文件存储、幂等与 CI 门禁此前已完成并持续有效。

## 10. 后续动作

- 输出上线 SOP。
- 输出回滚 SOP。
- 收口生产环境变量真实值。
- 明确首个生产管理员真实创建流程。
- 准备最终备份和上线窗口。
- `verify-api-login-dockerexec.sh` 的 `bcrypt` 依赖问题已通过脚本修复收口，可继续作为正式验收入口复用。
- API Dockerfile 已补齐 `python3/make/g++`，`release-smoke` 的 bcrypt 构建依赖已收口，后续 release build 可直接依赖该镜像链路。
