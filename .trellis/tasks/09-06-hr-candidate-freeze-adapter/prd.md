# Connect real T0-T3 candidates to frozen import preparation

## Goal

Connect real T0-T3 private candidates to existing frozen decisions and bridge for eventual production migration. Provide a usable private-file command, not another unused library.

## Requirements

- Authenticate all four producer-shaped candidates and matching phase bytes, complete inventory, scope and C/S/M. Preserve every source identity/row hash and explicit zero tables.
- Verified insert candidates convert automatically. Skip, merge and quarantine require externally reviewed resolutions with actual crypto metadata where needed. Never invent approval or transform collisions into implicit merge.
- Missing review evidence yields counted HOLD without partial executable decisions. Preserve original exception reasons and unresolved refs in authenticated retained evidence.
- No database, source extraction, signing, key discovery, activation or business writes. Preparation remains production HOLD.

## Acceptance Criteria

- [ ] All four real-producer shapes through adapter to existing bridge/generator, sixteen tables, deterministic hashes, unchanged inputs.
- [ ] Insert-only needs no reviews; non-insert missing/malformed evidence fails or HOLD. Real synthetic crypto covers non-insert success.
- [ ] Reject stale triple/scope/inventory/phase/T0 bindings, false counts, unsupported fields, collisions, missing/extra reviews and invalid refs.
- [ ] Private CLI uses bounded owned no-follow single-link IO, exclusive outputs, fsync/readback, completion receipt last and aggregate-only errors.
- [ ] New integration/IO tests plus existing bridge/generator tests, syntax and diff checks pass.

## Notes

- User authorized continuous implementation. No product choice blocks this bounded connection.
- External signature authenticity, production activation, T4/binaries, standalone acceptance and actual production writes are not established by this adapter.
