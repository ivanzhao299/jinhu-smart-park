# T3 private artifact materializer

Connect existing receipt-bound source staging to the normalized T3 phase/candidate assembler and produce actual private artifacts, not another detached mapping helper. Authenticate config/source manifest/target inventory/T0 candidates and every stage byte/hash/count. Preserve current C/S/M and production target scope; never relabel stale receipts. Output phase, candidates and policy recovery lineage together, with read-back hashes and a final completion receipt. No business DB, credentials, raw-source re-extraction or production writer.

Use bounded streaming input/output so the49MB insurance stage and expanded~250k records do not require a second whole JSON string/Buffer for file serialization. Existing pure assembler still retains structured records; do not claim fully streaming end-to-end execution. Test full synthetic private IO and bridge-compatible canonical phase bytes, all three source domains, current and reconstructed policies, zero domains, duplicate/missing parent conservation, exact source/target bindings, privacy, immutable sources, bounds, file permissions/links, write collisions and partial-output failure. Keep source data out of stdout and Git.
