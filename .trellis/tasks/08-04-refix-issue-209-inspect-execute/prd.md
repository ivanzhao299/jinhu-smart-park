# 复测修复 Issue 209 巡检执行无响应

## Goal

复测重新打开的 GitHub Issue #209：点击执行后必须进入并启动巡检业务流程，修复权限与状态链路，补充防复发测试和规范。

## Requirements

- “执行”是巡检业务动作入口，不得只做详情预加载后要求用户再次寻找“开始任务”。
- 待执行或已逾期任务点击“执行”后必须启动任务并进入执行抽屉；执行中的任务点击后直接恢复执行上下文。
- 已完成任务不再展示“执行”动作，只保留查看入口。
- 执行上下文必须由巡检任务权限闭环提供，不得额外依赖检查项管理读取权限。
- 请求失败必须保留在当前页面并显示可见错误；快速重复点击不得发起并发启动请求。
- 修复需覆盖 API/Web 数据契约、权限、状态转换和防复发测试，并记录同类业务动作入口规范。

## Acceptance Criteria

- [x] 待执行/逾期任务点击一次“执行”即调用启动接口、状态转为执行中并打开执行抽屉。
- [x] 执行中任务点击“执行”可打开含完整检查项的执行抽屉，且不会重复启动。
- [x] 已完成任务不显示“执行”按钮。
- [x] 具有巡检任务读取与执行权限但没有检查项管理读取权限的人员仍能进入执行流程。
- [x] 同一页面内快速重复点击只产生一次执行入口请求，按钮在请求期间有明确禁用反馈。
- [x] 详情/启动响应中的检查项和既有结果在进入表单前经过运行时投影校验，异常时不白屏。
- [x] 相关单测、API 全量单测、Web/API lint、typecheck、build 与 `git diff --check` 全部通过。

## Notes

- 来源：重新打开的 GitHub Issue #209 及 2026-08-04 复测评论。
- 不自动合并；提交 PR 后请求 Codex review，并按既定流程持续检查新反馈与 CI。
