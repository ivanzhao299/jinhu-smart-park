# 民宿 MVP 专项验证证据

> 初始执行日期：2026-07-24；本轮闭环复验：2026-08-20
> 本轮执行分支：`codex/homestay-completion-uat`
> 最终复审候选 SHA：`8012b854bcd2024b1f6bdfddf7cf13607d02bfc3`；合并后 main SHA：`619b8d20e891c74f69abcc9c908666034c37c648`
> 证据状态：隔离真实 API E2E、Chrome DevTools MCP 本地 UAT、PR/main CI、Release Smoke、生产健康检查、Docker 清理与公开生产保护账号校验全部通过；真人岗位 UAT 仍待外部执行

## 1. 覆盖范围

- 集中式公寓/人才公寓的整套短租经营。
- 基础日价、日期覆盖价、订单逐夜价格快照和简单取消规则。
- 自营直订、人工录单及 OTA 预留字段（不对接 OTA）。
- 共享房产底座经营模式校验和 `[start, end)` 统一占用互斥。
- 草稿临时锁房、确认、取消、改期、未到店、入住和退房。
- 共享个人档案、内部实名核验、人工钥匙/门卡/入住凭证。
- 人工费用、收款、退款和减免登记，不接在线支付网关。
- 退房自动生成保洁占用，保洁/复检完成后恢复可售。
- 今日到离店、在住、可营房源、入住率、平均房价、实收和待保洁。
- 运营后台与现场移动端页面；公开预订和住客自助端未开放。

## 2. 自动化验证

| 验证项 | 命令/入口 | 结果 |
|---|---|---|
| API TypeScript | `tsc -p apps/api/tsconfig.json --noEmit` | PASS |
| Web TypeScript | `tsc -p apps/web/tsconfig.json --noEmit` | PASS |
| 目标 lint | ESLint 覆盖 `apps/api/src/modules/homestay`、`apps/web/app/homestay` | PASS |
| API 构建 | `tsc -p apps/api/tsconfig.json` | PASS |
| 目标单元测试 | 编译后执行民宿、共享房产和数据库冲突 spec | 12 PASS / 0 FAIL |
| PostgreSQL 迁移 | 隔离 `postgres:16-alpine` 应用 `000177_homestay_mvp.sql` | PASS |
| SQL 业务回归 | [`scripts/e2e/homestay-mvp.sql`](../../scripts/e2e/homestay-mvp.sql) | PASS，事务末尾回滚 |
| 桌面端视觉 UAT | Chrome，1440 × 1000 | PASS |
| 现场移动端视觉 UAT | Chrome，390 × 844 | PASS |

SQL 回归已覆盖：

- 短租经营配置和订单逐夜价格快照。
- 同一整套房源的共享占用冲突。
- 人工费用、收款及余额口径。
- 退房释放预订占用并创建保洁占用。
- 保洁完成释放运营占用。

## 3. 已实现的控制点

- 所有订单写操作使用幂等拦截器；占用写入由数据库排他约束兜底。
- 订单详情和所有状态动作均执行租户、园区和房源数据范围校验。
- 确认订单自动生成房费应收；已确认订单改期自动登记增收或减免差价。
- 未到店只允许从已确认状态进入，并同步释放共享占用。
- 入住至少需要一个已内部核验住客和一个已发放凭证。
- 退房前必须回收全部仍在发放状态的凭证。
- 保洁照片必须是当前租户、园区、任务下的有效 `homestay_turnover` 文件。
- 未识别的保洁动作返回稳定的 400，不会误落入复检流程。

## 4. 桌面与移动端视觉复验

视觉测试使用 [`scripts/e2e/homestay-visual-uat-mock.mjs`](../../scripts/e2e/homestay-visual-uat-mock.mjs)
提供固定展示数据，只验证页面布局和操作入口，不替代真实 API、数据库和权限验收。

桌面端结果：

- 1440 × 1000 视口无横向溢出。
- 房态、价格、人工录单、订单、入住准备、财务流水和退房保洁区域完整显示。
- 桌面订单表格正常显示，移动卡片列表保持隐藏。
- 数字、日期、枚举和金额字段保留相应输入约束。

390px 现场移动端结果：

- 页面和六个主要业务面板宽度均收敛在 385px 内，无横向溢出。
- 桌面订单表格隐藏，订单卡片列表正常显示。
- 表单控件宽度由错误的约 760px 修正为 383px，不再被外层裁切。
- 页面主要操作按钮最小高度达到 44px。
- 入住准备、实名登记、凭证发放/回收、人工收退款和保洁操作入口均可访问。

复验过程中发现并修复了移动端表格与卡片重复显示、表单被桌面表格最小宽度撑开的问题。

## 5. 2026-08-20 闭环复验

- 隔离环境：Docker Compose 项目 `jinhu_homestay_api_e2e_r5`，PostgreSQL 16，数据库 `jinhu_property_api_e2e_homestay_r5_20260820`。
- 初始化：220 个迁移、生产与开发种子、共享房产 fixture 全部成功。
- 真实 API E2E：并发价格写入、下单、改期、到店日未到店、取消/强制释放审批、实名、凭证丢失/重放/回收、入住/退房、退款/减免独立审批、流水汇总、保洁文件/异常/完成/历史全部通过。
- 权限矩阵：独立审批人执行高风险审批；跨园区、模块启停、字段和文件边界由真实 API E2E 与现有权限/数据库契约测试联合验证。
- Chrome DevTools MCP：1440×900、768×900、390×844、360×800 均无横向溢出；移动卡片/桌面表格切换正确；结构化返回、键盘焦点、403、404、409、真实网络请求均通过。
- 可访问性：民宿工作面移动端交互控件最小 44×44px；民宿表单控件具备稳定 `name`；控制台仅保留模块外全局园区选择器的既有提示。
- 最终复审复验：审批列表按返回 URL 恢复页码，含 `%` 的结构化返回上下文不丢失；财务操作选项与后端订单状态矩阵一致，并在 Chrome DevTools MCP 验证第 2 页、已退房和草稿场景。
- 复审竞态回归：任务或审批写操作期间切换深链目标时，旧目标完成回调不会再触发旧加载或覆盖新页面状态。
- 自动化门禁：全仓 `test:unit`、lint、typecheck、build、民宿 API E2E 契约和 `git diff --check` 全部通过。
- 清理验证：隔离测试容器、网络、数据卷、临时文件目录和诊断文件残留均为零。

## 6. 最终候选与发布验证

- 最终增量：`8012b854` 相对完整 API/浏览器证据仅调整共享运行时的加载与 mutation 竞态保护；最终候选上的 Web property 契约 19/19、Web lint、Web typecheck 和 `git diff --check` 通过。
- Review/PR：Codex 对 `8012b854` 明确确认无重大问题；PR #331 的 CI 与 Release Smoke 全绿并合并。
- Main CI：run `32340203702` 全绿，覆盖 lint、typecheck、单测、构建、迁移、生产种子、登录、Property API E2E、日志上传与测试容器清理。
- Production：Deploy Production run `32340203683` 成功；完整健康检查和 API liveness 通过，Docker unused images/build cache 清理完成，6 个受保护账号经公开生产地址校验全部 PASS。

## 7. 当前验证边界

本轮是隔离本地真实数据库/API 与真实浏览器验收，不等同于业务人员在目标生产数据上的签字验收。
技术发布链已经完成，但不能替代 PR192 真人岗位泳道。仍需外部 UAT coordinator：准备并证明隔离 UAT 环境、角色账号和合成数据；通过批准渠道分发账号；冻结同一候选 SHA 下的 task-card、样本和 success/p90/interaction/error 阈值；每岗位至少招募 5 名真人代表、每人执行 4 个标准任务并覆盖桌面/390px；填写观察与缺陷台账；由产品/运营、业务、财务、安全、技术/运维和发布负责人作出具名签署与发布批准。在这些 H0/H1/H2 门槛完成前保持 `uat_pending/awaiting_human_gate`。
