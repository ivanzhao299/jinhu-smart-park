# Design

Keep storage and transport unchanged. Add an exhaustive shared map only for the three repository-observed standard values, then layer enabled tenant dictionary items over it in the existing housing display-dictionary hook. The presentation helper owns fallback behavior. List, detail, and form consume that helper/options so labels and submitted values cannot drift.
