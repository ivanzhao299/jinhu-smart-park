# v11 direct PostgreSQL fault-regression candidate

The v11 runner is self-contained and never imports or delegates to a prior runner. Static
candidate inspection issues no command. Actual execution requires immutable, SHA-bound
DB → QA → security → drain → resource authority files, plus a complete retained A–H/v4
identity registry. It uses a direct `000185`–`000195` migration baseline, then tests the
four injected rollback boundaries. Every child intent/result, terminal result and manifest
is created once as read-only evidence with child output suppressed.

This handoff is not a resource-creation authority and does not authorize `000197`.
