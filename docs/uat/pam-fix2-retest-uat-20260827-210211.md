# PAM-004 + PAM-005 修复复测 UAT 补完轮

## 结论

- RUN_ID：`20260827-210211`
- 被测 revision：`8909567270a36b18dec8bed6e75efc7ab035df70`（执行前最新 `origin/main`）
- 结论：**BLOCKED（UI fixture harness）/ 六项 Case 未开始 / 不归档**
- 本轮修正了上轮 Phase 0 与 login 预检违规：三份 Phase 0 工件在 Chrome 启动前完成；curl 与真实 Chrome login 预检均 PASS。
- UI fixture 同题两次失败后依约停止。没有用 DB 直写冒充 UI 建链，没有登录任何业务 fixture 账号，没有执行或断言六项业务 Case。

## Phase 0 门禁

Chrome 启动前已在 local-only 根 `/tmp/jinhu-pam-fix2-20260827-210211/preflight/` 完成并 SHA-256 留档：

| 工件 | SHA-256 |
| --- | --- |
| `design-basis.md` | `2cfaebd61f578f74af3b12af29086f57c0ccc6129cf5a21436c291c8e86c9b4e` |
| `design-implementation-audit.md` | `2b699f8bb5cf03b50c41cfc049d7998b39d56bfa46883e5b50f41ad36efcb65e` |
| `role-flow-matrix.md` | `c5bed55ec23636106af93e08fae7f6e99607f193c0463c8c3af4ed0c648d7ea6` |

工件覆盖 PAM-004/005 PRD/design/implement、园区切换契约与审计报告 §15 七组回归，并明确六项矩阵只能覆盖 §15 的子集。实现审计另披露：最终代码/测试冻结为 `menu_tree ?? menus`，与 PAM-004 design 的早期 `menus` 优先文字存在设计演进；不把该差异隐藏为完全一致。

## 隔离环境与初始化

- branch：`codex/pam-fix2-uat-evidence-20260827-210211`
- compose project：`jinhu-pam-fix2-uat-20260827-210211`
- PostgreSQL / API / Web / CDP：`55537 / 3161 / 3162 / 9471`，启动前均实测空闲。
- 新 database、volume、profile 与 fixture 前缀；未复用上轮 fixture/profile。
- 当前 main 全量 migration 成功，`sys_schema_migration_history` 为 264 条 `succeeded`；production-safe seed 成功；bootstrap 前仅预期的 admin 缺失，bootstrap 后 strict baseline PASS。
- `/api/v1/ready=200`；首次 `/login` 编译热身后 curl 返回 200，SSR 含 `signin-form`、username/password autocomplete 标记。

## 浏览器工具与正式 Case 前预检

- 本会话无可调用的 chrome-devtools MCP；用户在本轮简报预授权 raw CDP 等价工具，例外在启动前记录。
- Chrome `151.0.7922.138`，CDP protocol `1.3`，`--headless=new`，独立 profile。
- login 预检 PASS：title `金湖科创产业园 SaaS 平台`；body 含登录文案；form/username/password selector 均存在；`readyState=complete`；target URL `http://127.0.0.1:3162/login`。
- 实测 viewport：`1440×960`、DPR 1、`maxTouchPoints=10`、fine pointer=true、coarse=false。
- 截图：`screenshots/00-login-precheck.png`，SHA-256 `a5f3c601698d358892074110e3a9f5914b7b406d9162d843415c1d5f349098f6`（local-only）。已逐图人工确认无密码、token、Cookie、Authorization 或连接串。

## UI fixture 尝试

要求链路为：园区管理 UI 创建 Park B；角色管理 UI 创建 page/action 角色并配置权限；从 `HOUSING_OPERATOR` 模板实例化 Track-B；用户管理 UI 创建并分配账号。

1. 第一次：Park B 与 page role 已经由真实 UI 创建；权限树已找到并勾选 `housing:dashboard:page`，但 harness 使用错误按钮文案“保存权限树”，实际页面为“保存权限”，因此在保存前停止。
2. 第二次：harness 修正按钮文案，并尝试识别已有对象续跑；已有 page role 不在当前角色树首屏文本，错误进入新增分支，服务端未产生重复角色，harness 等待“角色已创建”超时。

DB 只读佐证证明第二次后仍仅 1 个本轮 page role 且权限关系为 0，没有 action/Track-B 角色或业务 fixture 用户。依“同题失败 ≤2”停止，不再扩大 workaround。Park B UI 证据截图 `fixture-park-b.png`，SHA-256 `fc5fa9f31b7f0e0e662fb6e56f537971298df5af053895355b015ffe2b9a8061`；截图只含本轮合成名称，无敏感信息。

## 六项矩阵

| Case | 结果 | 原因 |
| --- | --- | --- |
| PAM-004-DISABLED | BLOCKED / 未开始 | UI fixture 未完成 |
| PAM-005-ENABLED-LANDING | BLOCKED / 未开始 | 同上 |
| ACTION-ONLY | BLOCKED / 未开始 | 同上 |
| TRACK-B-TASK-DESK-ONLY | BLOCKED / 未开始 | 同上 |
| AUTH-AFTER-REFRESH | BLOCKED / 未开始 | 同上 |
| PAM-005-PARK-SWITCH | BLOCKED / 未开始 | 同上 |

业务 Case 重试消耗为 0；本报告不把 login 预检或部分 fixture 当作业务 PASS。PAM-004、PAM-005 子任务保持 `in_progress`，审计父任务保持未归档。§15 七组回归本轮未执行，原因是其前置六项 fixture 未建立。

## residual 与 teardown

- 精确 cleanup 前：`sys_user=1`、`sys_role=1`、`rel_user_role=2`、`rel_user_park=2`、`rel_role_perm=0`、本轮 `biz_park=1`。其中 user 是 bootstrap admin，role 是未完成的 page role。
- 首条 here-doc cleanup 因缺少 `docker exec -i` 未向 psql 送入 SQL；before/after 相同且日志为空，确认未发生部分删除。修正后在单事务中按本轮 username/code/park_code 精确删除，并补充删除 bootstrap admin 的 `rel_user_org` 与空的角色绑定表。
- 最终：上述五表与本轮 `biz_park` 全部为 0。
- API/Web/专用 Chrome 均停止；compose `down -v --remove-orphans` 完成；project container 为 0；`55537/3161/3162/9471` 监听均为 0。
- 未触碰 HR 系列、生产、主 Chrome或他人容器。

## 本地证据索引

local-only 根：`/tmp/jinhu-pam-fix2-20260827-210211/`

- Phase 0：`preflight/*.md`、`preflight/SHA256SUMS`
- 初始化：`migrate.log`、`seed.log`、`baseline-before.log`、`bootstrap.log`、`baseline-after.log`
- curl/浏览器预检：`curl-precheck.txt`、`login-precheck.json`、`login-precheck.log`、`screenshots/00-login-precheck.png`
- fixture：`ui-fixture.mjs`、`ui-fixture-failure.txt`、`ui-fixture-2.log`、`screenshots/fixture-park-b.png`
- residual/teardown：`residual-before.txt`、`residual-delete.log`、`residual-after.txt`、`compose-before-down.txt`、`compose-down.log`、`ports-after-cleanup.txt`、`containers-after.txt`
- 截图 manifest：`screenshots/SHA256SUMS`

## 下一轮唯一前置修正

fixture harness 必须用 API 分页或页面关键词查询定位已存在角色，再在 UI 选择它；不得以当前角色树首屏 `body.innerText` 判断是否存在。使用新 RUN_ID、新环境、新 profile，并重新执行本轮已经通过的 Phase 0 和 login 预检门禁。
