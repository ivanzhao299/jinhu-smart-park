# PAM-004 + PAM-005 修复复测 UAT 第五轮

## 结论

- RUN_ID：`20260828-095912`
- 被测 revision：`405968d96b7e3cc1d1f5278d547fd2c87e80a338`
- PostgreSQL / API / Web / CDP：`56557 / 3281 / 3282 / 9591`
- 结论：**PASS（六项矩阵 6/6）**

本轮仅重跑 `PAM-005-PARK-SWITCH`。fixture 使用产品原生方案：创建 Park B 时只给既有 bootstrap admin 追加 B access；管理员主园区始终为 A，随后通过 `POST /auth/switch-context` 切换 token active park 到 B。没有恢复、切换或 PATCH 同名 bootstrap admin，也没有创建同名管理员记录。

## Phase 0 与环境门禁

- local-only 证据根：`/tmp/jinhu-pam-fix5-20260828-095912/`。
- 独占 compose project：`jinhu-pam-fix5-uat-20260828-095912`；四个候选端口启动前为空，未触碰其他容器或主 Chrome。
- 265 个 migration（含 8 个 prerequisite）、production-safe seed、bootstrap admin、strict baseline 全部通过。
- API 初次 watch 编译检出本地 `@jinhu/shared` 生成物陈旧；仅执行 `pnpm --filter @jinhu/shared build`，随后 API 0 errors 启动。没有修改 HR 文件。
- `/api/v1/ready=200`、`/login=200`；真实 Chrome `151.0.7922.138`、CDP `1.3`、`1440×960` login selector/viewport 预检 PASS。
- Phase 0 三工件与 `preflight/SHA256SUMS` 已刷新；敏感 env/token 未进入报告或 manifest，teardown 时安全删除。

## Fixture 编排与 16 表冻结

Park 创建副作用冻结沿用 R4 的 16 表集合：`biz_park`、`sys_org`、`rel_tenant_module`、`sys_dict_type`、`sys_dict_item`、`sys_code_rule`、`asset_park`、`sys_property_runtime_control`、`sys_property_runtime_control_contract_audit`、`sys_permission`、`sys_role`、`rel_role_perm`、`rel_user_role`、`rel_user_park`、`rel_user_org`、`sys_op_log`。before/after fixture 计数分别落盘。

fixture 全部使用产品 API：

1. bootstrap admin 在 A scope 创建 Park B；产品事务自动追加 B access 和 B tenant-admin 关系，不改其主园区。
2. 创建 A scope page/action/Track-B 角色与用户；switch 用户先取得 A/B access，再用已在 R4 验证的 `PATCH /users/:id` 将主园区设为 B。
3. bootstrap admin 调用 `POST /auth/switch-context` 切换 active park 到 B；在 B scope 创建 `housing:tasks:page` 角色，并按已知用户 id 绑定 switch 用户，不依赖 B scope `GET /users` 列表投影。
4. 数据库只读证据确认 bootstrap admin 主园区仍为 `20000001`（A），switch 用户主园区为 Park B；不存在同名 admin 冲突路径。

## Park-switch 浏览器结果

runner 先以 switch 用户的主园区 B 登录，再通过产品 selector/switch-context 将浏览器置于 A scope，进入 legacy `/housing` 起点并清除 setup Network 事件；随后执行被测 A → B 切换。

PASS 门禁同时满足：

- `/users/me.park_id == Park B`；
- selector value `== Park B`；
- Network `POST /api/v1/auth/switch-context = 200`；
- 完成态 landing 为 `/housing/tasks`，且该 href 存在于 Park B Sidebar（Sidebar 为 `/dashboard`、`/housing/tasks`）。

截图：`screenshots/07-park-switch-normalized-fallback.png`；最终 UI logout 截图：`screenshots/08-final-ui-logout-login.png`。runner 最终 URL 为 `about:blank`，专用 Chrome profile 已删除。

## 六项矩阵终态

| Case | 结果 | 证据 |
| --- | --- | --- |
| PAM-004-DISABLED | PASS（沿用 R3） | `docs/uat/pam-fix3-retest-uat-20260827-220612.md` |
| PAM-005-ENABLED-LANDING | PASS（沿用 R3） | R3 browser/network evidence |
| ACTION-ONLY | PASS（沿用 R3） | R3 browser/network evidence |
| TRACK-B-TASK-DESK-ONLY | PASS（沿用 R3） | R3 browser/network evidence |
| AUTH-AFTER-REFRESH | PASS（沿用 R3） | R3 browser/network evidence |
| PAM-005-PARK-SWITCH | **PASS** | R5 `browser-results.json`、Network、before/after state 与 `07` 截图 |

六项矩阵最终为 **6 PASS / 0 BLOCKED**。

## 证据与 teardown

- 初始化：`migrate.log`、`seed.log`、`baseline-before.log`、`bootstrap.log`、`baseline-after.log`。
- 预检：`ready-body.json`、`login-precheck.json`、`screenshots/00-login-precheck.png`。
- fixture：`api-fixture-results.json`、`api-fixture.log`、`network/api-fixture-network.json`。
- Park-switch：`browser-results.json`、`park-switch-before.json`、`park-switch-after.json`、`network/browser-cases-network.json`、`screenshots/07-park-switch-normalized-fallback.png`、`screenshots/08-final-ui-logout-login.png`。
- DB：`db/park-create-tables-before.txt`、`db/park-create-tables-after-fixture.txt`、`db/user-primary-parks.txt`、`db/park-create-tables-after-cleanup.txt`。
- 哈希：`preflight/SHA256SUMS`、`screenshots/SHA256SUMS`、`evidence-SHA256SUMS`。

完整 teardown 已执行：UI logout → `/login` → `about:blank` → `Browser.close` → profile 删除；API/Web 停止；本轮 compose container、volume、network 销毁；四端口归零。16 表 cleanup 以独占数据库/volume 不存在为精确零残留边界记录。未触碰生产、HR 文件、他人容器或主 Chrome。

## 任务状态与遗留

- PAM-004、PAM-005 已满足总矩阵 PASS 门禁，可归档。
- §15 七组回归本轮未执行；权限审计父任务如实保持未归档。

