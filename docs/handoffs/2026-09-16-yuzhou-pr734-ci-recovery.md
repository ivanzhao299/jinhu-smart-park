# PR #734 CI 恢复与主线同步

- 本轮确认 CI `34460637974` 已失败，失败步骤为 API `Unit tests`，不是仍在发布或等待 GitHub。
- 根因：`hr-employee-profile-materialization.contract.spec.ts` 仍在原 transform 文件搜索已提取到 `t5-nonfile-field-projection.mjs` 的未知字段/技能等级规则。
- 修复保留原安全断言，并增加 transform 对共享模块的 import 和调用接线断言；禁止把未知技能等级直接当成现代 proficiency 的断言同时覆盖两处源码。
- 已将 `origin/main` 的 `d15c22b4` 合并进现有候选，无冲突；未创建或清理其他工作树。主线新增的 T0 岗位关系处理、迁移 000314 及 9 月 11 日候选/验证交接保留。
- 9 月 11 日切片 D 文档报告 259,220 条隔离读回通过，但其范围为 T0–T3，且描述的是一次性脚本的候选/读回比对。不得据标题将它自动等同于当前受控全域双独立运行、回滚、T5/T4 或生产写入证明；下一步定位对应机器证据后复用，避免重跑。
- 生产导入仍未在本轮执行；没有重抽源数据、重做全量 A/B、更改历史回执或读取个人明细。
- 本地验证：失败所在契约 3 项通过；T0 候选、T5 受控读取及私有 stage 3 个入口合计 12 项通过；API `test:unit` 完整命令退出 0，API `typecheck` 退出 0，修改测试文件 ESLint 与 `git diff --check` 通过。未运行本地全仓构建或真实数据库装载；完整发布门禁交由修复后的 PR CI，未宣称 CI 已成功。

后续顺序：修复提交通过 CI → 核对候选发布身份 → 复用已有 T0–T3 与 T5 材料补齐实际入口/读回缺口 → 当前生产备份与受控执行。不能把文档状态或代码测试当成生产导入成功。

## T5 准备入口的真实 T0 状态兼容

后续审查发现此前隔离父测试使用了生产者不会输出的状态组合：有 quarantine 但 status 仍为 READY_FOR_FREEZE。真实 T0 生成器此时返回 REVIEW_HOLD，因此只删除 quarantine=0 条件仍不能消费真实候选。

现改为按实际计数校验状态：无隔离为 READY_FOR_FREEZE；有隔离且无目标冲突为 REVIEW_HOLD。未解决目标冲突、计数漂移、状态与计数不一致仍拒绝。隔离父不进员工索引、关联子记录无业务 payload 且保持 quarantine，输出始终 HOLD，不批准隔离数据或生产执行。CLI 回归改用真实生产者状态并增加虚假 READY 状态拒绝断言；两组 4 项测试通过。无需重抽旧数据。
