# HR M5 实施计划

## 1. 基线与权限

- [x] 复核 `000238`、T2 loader 总账和现有实体字段一致性。
- [x] 增加合同页面、园区/团队/本人读取和管理权限及最小角色 seed。
- [x] 增加菜单、路由和跨层权限契约测试。

## 2. API 产品化

- [ ] 修正合同实体与数据库列映射，增加 DTO 和显式投影。
- [x] 实现 fail-closed 数据范围、分页筛选、本人/团队/园区列表与详情。
- [ ] 实现在线合同及变更草稿的事务、锁、幂等、状态和历史不可变规则。
- [ ] 增加敏感读取/写入审计且不捕获正文或金额。

## 3. Web 工作台

- [x] 新增合同列表、到期提醒、筛选和详情变更链。
- [ ] 新建与续签/变更表单显式展开并按动作权限显示。
- [ ] 接入 HR 首页与员工生命周期详情，提供手机卡片布局。

## 4. 数据库与发布门禁

- [ ] 重扫迁移编号，创建前向迁移并执行 fresh PostgreSQL + production seed。
- [x] 验证 T2 802/357 历史总账、映射和回滚契约无回归。
- [x] 运行只读切片 HR/API/Web、lint/typecheck/build/security 和 diff 门禁。
- [ ] 桌面、手机、HR/负责人/员工只读验收；三端一致后合并部署。

## Rollback points

- 历史行不做反向变更；新 schema 仅前向。
- 在线写入功能与只读展示分提交，可在不删除历史数据的前提下回退应用版本。
- 生产迁移或 seed 任一失败即停止，禁止继续部署或人工跳过。

## 只读切片证据（2026-08-24）

- Fresh PostgreSQL：234/234 迁移成功；production seed 连续回放两次成功。
- 合同权限矩阵：HR 负责人园区读取，部门负责人团队+本人读取，员工仅本人读取；无重复关系。
- PostgreSQL 服务级门禁：跨园区合同同形态拒绝；列表/详情/变更均为精确 allowlist，工资、旧路径、源快照和审计字段不出站。
- 待完成：在线合同/变更草稿、写审计、显式办理表单、员工详情入口、角色浏览器验收及三端发布。
