# T3 legacy policy recovery

Recover the complete policy definition from the existing receipt-bound old six-key staged items, without re-extraction or source mutation. The historical transform stored suffix_2 fixed addends in variant=2 rate-labelled fields; current policy normalization correctly combines percentage points/100 plus separate fixed amounts into variant=1. Real private proof already reconstructed all 51 raw fields for 12 policies and matched every original row SHA, yielding 72 normalized items from 144 old projections.

Implement an exact, pure, per-policy recovery adapter. It must verify all raw-source fields against the inherited original row hash before normalizing, preserve source identity, and produce explicit two-old-to-one-new lineage using existing phase identity formulas. No null fabrication, rounding, amount coercion, automatic approval, archive/skip disposition or original stage rewriting. Accept only six known kinds with both variants and exact old item shape. Reject current shape to prevent double conversion, partial/mixed/unknown shapes, duplicates, incomplete kinds, unsupported amount types and raw hash drift.

Acceptance includes source immutability, all 51 fields conserved, null/zero/signed fixed values, exact percentage normalization, deterministic order, complete lineage for 12 old child identities to six new child identities, and direct integration with current pure field projector. The downstream phase/candidate owner must explicitly select normalized projection and updated counts under current evidence; an old phase artifact cannot be relabeled. This task does not itself execute production or claim full HR parity.
