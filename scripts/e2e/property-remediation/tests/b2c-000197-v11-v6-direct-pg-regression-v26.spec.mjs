import assert from "node:assert/strict";
import { spawnSync } from "node:child_process";
import { createHash } from "node:crypto";
import { chmodSync, copyFileSync, cpSync, existsSync, lstatSync, mkdirSync, mkdtempSync, readFileSync, readdirSync, renameSync, rmSync, statSync, symlinkSync, unlinkSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import test, { afterEach } from "node:test";
import { fileURLToPath, pathToFileURL, URL } from "node:url";

const sourceRoot = resolve(fileURLToPath(new URL("../../../../", import.meta.url)));
const runnerRelative = "scripts/e2e/property-remediation/track-b2c-000197-v11-v6-direct-pg-regression-v26.mjs";
const specRelative = "scripts/e2e/property-remediation/tests/b2c-000197-v11-v6-direct-pg-regression-v26.spec.mjs";
const fixtureRelative = "scripts/e2e/property-remediation/fixtures/b2c-000197-r0-two-scope-fixture-v1.sql";
const researchRelative = ".trellis/tasks/07-30-pr192-b-domain-integrations/research";
const prefix = "b2c-000197-v11-v6-direct-pg-regression-v26";
const sha = (value) => createHash("sha256").update(value).digest("hex");
const ids = Object.freeze({ run: "b2c197_v11v6_direct_v26_attempt23", container: "jinhu-b2c197-v11v6-direct-v26-attempt23", database: "jinhu_b2c197_v11v6_direct_v26_db23", containerId: "a".repeat(64), volumeId: "b".repeat(64), imageId: "c".repeat(64) });
const failedV15 = Object.freeze({ label: "v15", run_id: "b2c197_v11v6_direct_v15_20260803a", container: "jinhu-b2c197-v11v6-direct-v15-20260803a", container_id: "366d42084ff57b7ddb16bdf0ca30e39cb9a06a16d4eb9d6f9b139f70b2cdae99", database: "jinhu_b2c197_v11v6_direct_v15_20260803a", volume: "acea395306dcd510508dd5b9c97a89f3ce891b5e7b99b4984eba47d181a7b72a" });
const failedV17 = Object.freeze({ label: "v17", run_id: "b2c197_v11v6_direct_v17_20260803a", container: "jinhu-b2c197-v11v6-direct-v17-20260803a", container_id: "1ae79773a7a3133fe8fa5f99a3c06094f733b2b1f71b910225123b17640f9e63", database: "jinhu_b2c197_v11v6_direct_v17_20260803a", volume: "771024a7b887becf7af2fc05bcd0b9d32f54ef657fc5cab112437d44af4d3a87" });
const failedV18 = Object.freeze({ label: "v18", run_id: "b2c197_v11v6_direct_v18_20260803a", container: "jinhu-b2c197-v11v6-direct-v18-20260803a", container_id: "60ea2211fccbc66e518ebfb1b52bf164ed11a7d6e570245812913b08f81c4f5d", database: "jinhu_b2c197_v11v6_direct_v18_20260803a", volume: "11840d16e23a6e536f89ca9c070ec7423a0729050ea3d0ade69ef8140a10db09" });
const failedV19 = Object.freeze({ label: "v19", run_id: "b2c197_v11v6_direct_v19_20260803a", container: "jinhu-b2c197-v11v6-direct-v19-20260803a", container_id: "601b46601dfffc7c6945b6c104295f6a5bb88d36cc8e1b8dc423d2d0f29e95f2", database: "jinhu_b2c197_v11v6_direct_v19_20260803a", volume: "ab7708227745a13baa9292b8ecc28cb8426c0f9c22dfa02ced26170f2f8f5c75" });
const failedV20 = Object.freeze({ label: "v20", run_id: "b2c197_v11v6_direct_v20_20260803a", container: "jinhu-b2c197-v11v6-direct-v20-20260803a", container_id: "5652c9859fafbbd14cecf4d34ee6a20cb39d6ce17a20ce5f6a480edcb3d01a8e", database: "jinhu_b2c197_v11v6_direct_v20_20260803a", volume: "52d740c50514bb6a8299023a3227cb59a3d64b0a5f7d0391c46845dd51e930c8" });
const failedV21 = Object.freeze({ label: "v21", run_id: "b2c197_v11v6_direct_v21_20260803a", container: "jinhu-b2c197-v11v6-direct-v21-20260803a", container_id: "dd02cc04b494cba41cbbc3485bbe38f978c9528ad7493b8521ebc23fc962eb9b", database: "jinhu_b2c197_v11v6_direct_v21_20260803a", volume: "cbe5e8cb74bcc40f08a6f60b3016cbd60030295d06515e9e5985e0d47a841fa5" });
const failedV22 = Object.freeze({ label: "v22", run_id: "b2c197_v11v6_direct_v22_20260803a", container: "jinhu-b2c197-v11v6-direct-v22-20260803a", container_id: "11346c95fe2668b707135b976bd23791127d29b47d2d26190e8de0dac1ef0c5e", database: "jinhu_b2c197_v11v6_direct_v22_20260803a", volume: "8456f3edd0cf5e82ddb0761f232091da0f4a16ad495298aa2ac9ce696ef8bc54" });
const failedV23 = Object.freeze({ label: "v23", run_id: "b2c197_v11v6_direct_v23_20260803a", container: "jinhu-b2c197-v11v6-direct-v23-20260803a", container_id: "ad31d846d0096d0b9d48246361ea1e1ad20c90ea53756b08e8078192c34d85f4", database: "jinhu_b2c197_v11v6_direct_v23_20260803a", volume: "81c065e3c685e08b15f54909b8ca4b31887816aefda16b5f7f885889e48d160e" });
const failedV25 = Object.freeze({ label: "v25", run_id: "b2c197_v11v6_direct_v25_20260803a", container: "jinhu-b2c197-v11v6-direct-v25-20260803a", container_id: "e8cb0f0f1715af045ae4ea9a1dc625f963b1dc33cd47867bef93cce567d38f83", database: "jinhu_b2c197_v11v6_direct_v25_20260803a", volume: "fb5da03acebf1d199c89a8a4beb0cdf078ba0ec885aec2b22aceb5c98bc81fb4" });
const names = Object.freeze({
  candidate: `${prefix}-candidate-authority-20260803.json`, manifest: `${prefix}-candidate-manifest-20260803.json`, registry: `${prefix}-resource-registry-20260803.json`, staticRecord: `${prefix}-static-test-record-20260803.json`, node22Tap: `${prefix}-node22.tap`, node24Tap: `${prefix}-node24.tap`, databaseReview: `${prefix}-independent-database-review-20260803.json`, qaReview: `${prefix}-independent-qa-review-20260803.json`, securityReview: `${prefix}-independent-security-review-20260803.json`, drainReview: `${prefix}-independent-old-writer-drain-review-20260803.json`, provisionAuthority: `${prefix}-provision-authority-20260803.json`, observation: `${prefix}-resource-observation-20260803.json`, executionAuthority: `${prefix}-execution-authority-20260803.json`,
});
const reviewChain = ["candidate", "database", "qa", "security", "drain", "provision_authority", "resource_observation", "execution_authority"];
const fixtureRoots = new Set();
function makeRemovable(path) { if (!existsSync(path) || lstatSync(path).isSymbolicLink()) return; if (lstatSync(path).isDirectory()) { chmodSync(path, 0o700); for (const entry of readdirSync(path)) makeRemovable(resolve(path, entry)); } }
function recursiveFixtureFiles(directory) { return readdirSync(directory, { withFileTypes: true }).flatMap((entry) => entry.isDirectory() ? recursiveFixtureFiles(resolve(directory, entry.name)) : [resolve(directory, entry.name)]); }
afterEach(() => { for (const path of fixtureRoots) { makeRemovable(path); rmSync(path, { recursive: true, force: true }); } fixtureRoots.clear(); });

function json(value) { return `${JSON.stringify(value, null, 2)}\n`; }
function seal(path, value) { mkdirSync(dirname(path), { recursive: true }); if (existsSync(path) && !lstatSync(path).isSymbolicLink()) chmodSync(path, 0o644); writeFileSync(path, typeof value === "string" ? value : json(value)); chmodSync(path, 0o444); return sha(readFileSync(path)); }
function edit(path, mutate) { const value = JSON.parse(readFileSync(path, "utf8")); return seal(path, mutate(value)); }
function pathOf(fixture, key) { return resolve(fixture.research, names[key]); }
function canonicalPaths() { const result = {}; for (const [key, name] of Object.entries(names)) result[key] = `${researchRelative}/${name}`; return result; }

async function fixture({ migrations = false, registryMutator = (resources) => resources } = {}) {
  const root = mkdtempSync("/tmp/b2c-v26-test-"); fixtureRoots.add(root); const research = resolve(root, researchRelative); mkdirSync(resolve(root, dirname(runnerRelative)), { recursive: true }); mkdirSync(resolve(root, dirname(specRelative)), { recursive: true }); mkdirSync(research, { recursive: true });
  copyFileSync(resolve(sourceRoot, runnerRelative), resolve(root, runnerRelative)); copyFileSync(resolve(sourceRoot, specRelative), resolve(root, specRelative)); chmodSync(resolve(root, runnerRelative), 0o444); chmodSync(resolve(root, specRelative), 0o444); if (migrations) { cpSync(resolve(sourceRoot, "database"), resolve(root, "database"), { recursive: true }); mkdirSync(resolve(root, dirname(fixtureRelative)), { recursive: true }); copyFileSync(resolve(sourceRoot, fixtureRelative), resolve(root, fixtureRelative)); } else { mkdirSync(resolve(root, "database/migrations"), { recursive: true }); copyFileSync(resolve(sourceRoot, "database/migrations/000197_property_approval_active_source_index_forward_fix.sql"), resolve(root, "database/migrations/000197_property_approval_active_source_index_forward_fix.sql")); }
  mkdirSync(resolve(root, "apps/api/src/modules"), { recursive: true }); cpSync(resolve(sourceRoot, "apps/api/src/modules/property-approvals"), resolve(root, "apps/api/src/modules/property-approvals"), { recursive: true }); copyFileSync(resolve(sourceRoot, "apps/api/package.json"), resolve(root, "apps/api/package.json")); copyFileSync(resolve(sourceRoot, "apps/api/tsconfig.json"), resolve(root, "apps/api/tsconfig.json"));
  mkdirSync(resolve(root, "packages/shared"), { recursive: true }); cpSync(resolve(sourceRoot, "packages/shared/src"), resolve(root, "packages/shared/src"), { recursive: true }); cpSync(resolve(sourceRoot, "packages/shared/dist"), resolve(root, "packages/shared/dist"), { recursive: true }); copyFileSync(resolve(sourceRoot, "packages/shared/package.json"), resolve(root, "packages/shared/package.json")); cpSync(resolve(sourceRoot, "packages/config"), resolve(root, "packages/config"), { recursive: true }); for (const filename of ["package.json", "pnpm-lock.yaml", "pnpm-workspace.yaml", "tsconfig.base.json"]) copyFileSync(resolve(sourceRoot, filename), resolve(root, filename)); copyFileSync(resolve(sourceRoot, "scripts/e2e/property-remediation/track-b2c-approval-port-pg-gate-lib.cjs"), resolve(root, "scripts/e2e/property-remediation/track-b2c-approval-port-pg-gate-lib.cjs"));
  for (const name of ["@nestjs/common", "pg", "reflect-metadata", "ts-node", "typeorm", "typescript"]) { const directory = resolve(root, "node_modules", name); mkdirSync(directory, { recursive: true }); writeFileSync(resolve(directory, "package.json"), json({ name, version: "0.0.0-fixture", main: "index.js" })); writeFileSync(resolve(directory, "index.js"), "module.exports = {};\n"); if (name === "ts-node") writeFileSync(resolve(directory, "register.js"), "module.exports = {};\n"); }
  const runtimeExecutable = resolve(root, "runtime-node-fixture"); writeFileSync(runtimeExecutable, "v26 fixture node executable bytes\n"); chmodSync(runtimeExecutable, 0o755);
  const rawModule = await import(`${pathToFileURL(resolve(root, runnerRelative)).href}?fixture=${encodeURIComponent(root)}`), runtimeDependencySha = rawModule.runtimeDependencyManifestV26(), snapshotAuthority = rawModule.snapshotAuthorityV26();
  const tap22 = seal(pathOf({ research }, "node22Tap"), "TAP version 13\n1..1\nok 1 - v26 static Node 22\n"); const tap24 = seal(pathOf({ research }, "node24Tap"), "TAP version 13\n1..1\nok 1 - v26 static Node 24\n");
  const runnerSha = sha(readFileSync(resolve(root, runnerRelative))), specSha = sha(readFileSync(resolve(root, specRelative)));
  const historicalRuns = ["b2c197_r0_20260802a", "b2c197_r0_20260802a", "b2c197_prelim_20260802b", "b2c197_prelim_20260802b", "b2c197_prelim_20260802f", "b2c197_prelim_20260802f", "b2c197_prelim_20260802g", "b2c197_prelim_20260802g", "b2c197_v11v6_isolated_20260802a"];
  const resources = registryMutator([...(["A", "B", "C", "D", "E", "F", "G", "H", "v4"].map((label, index) => ({ label, run_id: historicalRuns[index], container: `past-container-${index}`, container_id: `${index}`.repeat(64), database: `past-database-${index}`, volume: `past-volume-${index}` }))), { ...failedV15 }, { ...failedV17 }, { ...failedV18 }, { ...failedV19 }, { ...failedV20 }, { ...failedV21 }, { ...failedV22 }, { ...failedV23 }, { ...failedV25 }]);
  const registrySha = seal(pathOf({ research }, "registry"), { schema_version: "b2c-v26-resource-registry-v1", resources });
  const recordSha = seal(pathOf({ research }, "staticRecord"), { schema_version: "b2c-v26-static-test-record-v1", runner_raw_sha256: runnerSha, test_raw_sha256: specSha, node22: { binary: "/fixture/node22", result: "pass", raw_tap_sha256: tap22 }, node24: { binary: "/fixture/node24", result: "pass", raw_tap_sha256: tap24 }, docker_or_database_command_executed: false, coverage: ["fixed-paths", "two-phase-authority", "exact-contracts", "fake-full-path"] });
  const migration197Sha = "a9b98ca82aa4dafc16535085184df838880ef27801f7cd4b225e1ca1a15af059", formalSourceSha = "b536de67faf6d3559974ce60d21a073bd51bcf28e4d1484c966954b19461e0d7";
  const candidateSha = seal(pathOf({ research }, "candidate"), { schema_version: "b2c-v26-candidate-authority-v1", status: "PENDING_REVIEW", execution_authorized: false, container_create_authorized: false, container_execute_authorized: false, formal_go: false, docker_or_database_command_executed: false, runner_raw_sha256: runnerSha, runner_spec_raw_sha256: specSha, registry_raw_sha256: registrySha, static_test_record_raw_sha256: recordSha, node22_tap_raw_sha256: tap22, node24_tap_raw_sha256: tap24, migration_000197_raw_sha256: migration197Sha, formal_source_manifest_raw_sha256: formalSourceSha, runtime_dependency_manifest_raw_sha256: runtimeDependencySha, ...snapshotAuthority });
  const manifestSha = seal(pathOf({ research }, "manifest"), { schema_version: "b2c-v26-candidate-manifest-v1", status: "PENDING_REVIEW", candidate_flags: { container_create_authorized: false, container_execute_authorized: false, execution_authorized: false, formal_go: false }, candidate_authority_raw_sha256: candidateSha, runner_raw_sha256: runnerSha, runner_spec_raw_sha256: specSha, registry_raw_sha256: registrySha, static_test_record_raw_sha256: recordSha, node22_tap_raw_sha256: tap22, node24_tap_raw_sha256: tap24, migration_000197_raw_sha256: migration197Sha, formal_source_manifest_raw_sha256: formalSourceSha, runtime_dependency_manifest_raw_sha256: runtimeDependencySha, ...snapshotAuthority, review_chain: reviewChain, canonical_paths: canonicalPaths(), trust_root: "repository-fixed-path-procedural-root-v1" });
  const common = { candidate_authority_raw_sha256: candidateSha, candidate_manifest_raw_sha256: manifestSha, registry_raw_sha256: registrySha, static_test_record_raw_sha256: recordSha, node22_tap_raw_sha256: tap22, node24_tap_raw_sha256: tap24, migration_000197_raw_sha256: migration197Sha, formal_source_manifest_raw_sha256: formalSourceSha, runtime_dependency_manifest_raw_sha256: runtimeDependencySha, ...snapshotAuthority };
  const databaseSha = seal(pathOf({ research }, "databaseReview"), { schema_version: "b2c-v26-independent-database-review-v1", decision: "GO", review_approved: true, open_p0: 0, open_p1: 0, ...common });
  const qaSha = seal(pathOf({ research }, "qaReview"), { schema_version: "b2c-v26-independent-qa-review-v1", decision: "GO", review_approved: true, open_p0: 0, open_p1: 0, ...common, database_review_raw_sha256: databaseSha });
  const securitySha = seal(pathOf({ research }, "securityReview"), { schema_version: "b2c-v26-independent-security-review-v1", decision: "GO", review_approved: true, open_p0: 0, open_p1: 0, ...common, database_review_raw_sha256: databaseSha, qa_review_raw_sha256: qaSha });
  const drainSha = seal(pathOf({ research }, "drainReview"), { schema_version: "b2c-v26-independent-old-writer-drain-review-v1", decision: "GO", review_approved: true, open_p0: 0, open_p1: 0, ...common, database_review_raw_sha256: databaseSha, qa_review_raw_sha256: qaSha, security_review_raw_sha256: securitySha });
  const provisionSha = seal(pathOf({ research }, "provisionAuthority"), { schema_version: "b2c-v26-provision-authority-v1", status: "AUTHORIZED", container_create_authorized: true, container_execute_authorized: false, execution_authorized: false, formal_go: false, candidate_authority_raw_sha256: candidateSha, candidate_manifest_raw_sha256: manifestSha, database_review_raw_sha256: databaseSha, qa_review_raw_sha256: qaSha, security_review_raw_sha256: securitySha, drain_review_raw_sha256: drainSha, runtime_dependency_manifest_raw_sha256: runtimeDependencySha, ...snapshotAuthority, run_id: ids.run, container: ids.container, database: ids.database, image_reference: "postgres:16-alpine", host_port_bindings: 0, anonymous_volume: true, mount_type: "volume", mount_rw: true, mount_destination: "/var/lib/postgresql/data" });
  const module = { ...rawModule, provisionV26: ({ runCommand }) => rawModule.createIsolatedV26TestHarness(runCommand).provision(), executeV26: ({ runCommand }) => rawModule.createIsolatedV26TestHarness(runCommand).execute() };
  return { root, research, module, rawModule, runtimeExecutable, hashes: { candidateSha, manifestSha, databaseSha, qaSha, securitySha, drainSha, provisionSha, runtimeDependencySha, ...snapshotAuthority } };
}

function inspectOutput(fixture, extraLabels = {}, image = `sha256:${ids.imageId}`) { return `${ids.containerId}\ntrue\n${image}\n{}\n${JSON.stringify([{ Type: "volume", Name: ids.volumeId, RW: true, Destination: "/var/lib/postgresql/data" }])}\n${JSON.stringify({ "jinhu.b2c.run_id": ids.run, "jinhu.b2c.provision_authority_sha256": fixture.hashes.provisionSha, "desktop.docker.io/wsl-distro": "Ubuntu-26.04", ...extraLabels })}\n`; }
function assertRealInspectFormat(argv) { assert.equal(argv[2].includes("\\n"), false); assert.equal((argv[2].match(/\n/gu) ?? []).length, 5); }
function provisionFake(fixture, { failInspect = false, extraLabels = {}, secret = "", image = `sha256:${ids.imageId}` } = {}) { return (command, argv) => { assert.equal(command, "/usr/bin/docker"); if (argv[0] === "run") return { status: 0, stdout: `${ids.containerId}\n`, stderr: "" }; if (argv[0] === "inspect") { assertRealInspectFormat(argv); return failInspect ? { status: null, stdout: "", stderr: "", error: new Error(secret || "inspect failed") } : { status: 0, stdout: inspectOutput(fixture, extraLabels, image), stderr: "" }; } throw new Error(`unexpected provision command:${argv.join(" ")}`); }; }
function completeExecutionAuthority(fixture) {
  const observationPath = pathOf(fixture, "observation"), observationSha = sha(readFileSync(observationPath));
  seal(pathOf(fixture, "executionAuthority"), { schema_version: "b2c-v26-execution-authority-v1", status: "AUTHORIZED", container_create_authorized: false, container_execute_authorized: true, execution_authorized: true, formal_go: false, candidate_authority_raw_sha256: fixture.hashes.candidateSha, candidate_manifest_raw_sha256: fixture.hashes.manifestSha, database_review_raw_sha256: fixture.hashes.databaseSha, qa_review_raw_sha256: fixture.hashes.qaSha, security_review_raw_sha256: fixture.hashes.securitySha, drain_review_raw_sha256: fixture.hashes.drainSha, provision_authority_raw_sha256: fixture.hashes.provisionSha, resource_observation_raw_sha256: observationSha, runtime_dependency_manifest_raw_sha256: fixture.hashes.runtimeDependencySha, combined_snapshot_raw_sha256: fixture.hashes.combined_snapshot_raw_sha256, resolution_guard_raw_sha256: fixture.hashes.resolution_guard_raw_sha256, run_id: ids.run, container: ids.container, container_id: ids.containerId, database: ids.database, volume_id: ids.volumeId, image_id: ids.imageId });
}
const lateNames = ["000185_property_b_identity_schema_expand.sql", "000186_property_b_approval_runtime_schema.sql", "000187_property_b_event_notification_schema.sql", "000188_property_b_task_runtime_schema.sql", "000189_property_b_module_rbac_definitions.sql", "000190_property_b_migration_compatibility_control.sql", "000193_property_b_runtime_integrity_forward_fix.sql", "000194_property_task_projection_contract_correction.sql", "000195_property_mutation_receipt_contract_v2.sql"];
function exactSnapshot(fixture) { const history = lateNames.map((filename) => ({ filename, raw_sha256: sha(readFileSync(resolve(fixture.root, "database/migrations", filename))), status: "succeeded" })); return { primary_history: history, mirror_history: history, forbidden_primary: 0, forbidden_mirror: 0, approval_rows: 0, keys: ["tenant_id", "park_id", "action_id", "source_type", "source_id", "source_expected_version"], indexdef: "89d630118eeeab3655fffe97cde034d82567c1e253c543f9a33a7a8420768584", predicate: "d47740fefda3dc305edc9f845c359dfae15d6d9fa1c28a096870870129563a37", catalog: { index_exists: true, build_residue: false } }; }
function exactFinalSnapshot(fixture) { const base = exactSnapshot(fixture), migration = { filename: "000197_property_approval_active_source_index_forward_fix.sql", raw_sha256: "a9b98ca82aa4dafc16535085184df838880ef27801f7cd4b225e1ca1a15af059", status: "succeeded" }, history = [...base.primary_history, migration].sort((left, right) => left.filename < right.filename ? -1 : left.filename > right.filename ? 1 : 0); return { primary_history: history, mirror_history: history, forbidden_primary: 0, forbidden_mirror: 0, approval_rows: 0, keys: base.keys, indexdef: "dd004f0c2e5f40e86ec1953effa91b8604614e276c9fedabe7f2464f13d70d9c", predicate: "24ef911486d5274d6c439d63de6aa253b289241ac2b75317b1f98bc93a5a8fda", old_predicate_matches: 0, catalog: { index_exists: true, build_residue: false } }; }
const approvalNames = ["requires the forward-fixed active partial unique predicate", "recovers every real dependent 23505 and proves caller commit or rollback", "fails unknown 23505 and unknown DB errors closed with usable caller manager", "keeps writes invisible before caller commit and removes them on caller rollback", "enforces terminal monotonicity before INSERT under the caller-held source lock", "serializes two post-terminal intents with the caller-held source lock", "resolves client-key, business-intent and active-source races and preserves manager usability"];
function approvalTap() { return `TAP version 13\n${approvalNames.map((name, index) => `# Subtest: ${name}\nok ${index + 1} - ${name}`).join("\n")}\n1..7\n# tests 7\n# suites 0\n# pass 7\n# fail 0\n# cancelled 0\n# skipped 0\n# todo 0\n`; }
function executionFake(fixture, { extraLabels = {} } = {}) { const snapshot = JSON.stringify(exactSnapshot(fixture)), finalSnapshot = JSON.stringify(exactFinalSnapshot(fixture)), approvalRunId = sha(ids.run).slice(0, 32); return (command, argv, options = {}) => { if (command === "/usr/bin/docker") { if (argv[0] === "inspect") { if (argv[2] === "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}") return { status: 0, stdout: "172.18.0.2\n", stderr: "" }; assertRealInspectFormat(argv); return { status: 0, stdout: inspectOutput(fixture, extraLabels), stderr: "" }; } const input = String(options.input ?? ""); if (input === "SHOW server_version_num;") return { status: 0, stdout: "160010\n", stderr: "" }; const marker = input.match(/v26-injected-(before-create|after-create|after-drop|before-rename)/u)?.[0]; if (marker) return { status: 1, stdout: "", stderr: `ERROR:  P0001: ${marker}\n` }; if (input.includes("SELECT json_build_object('primary_history'")) return { status: 0, stdout: `${input.includes("'old_predicate_matches'") ? finalSnapshot : snapshot}\n`, stderr: "" }; return { status: 0, stdout: "", stderr: "" }; } assert.equal(command, fixture.runtimeExecutable); const phase = argv.at(-1); if (["probe", "setup", "cleanup"].includes(phase)) return { status: 0, stdout: `${JSON.stringify({ phase, runId: approvalRunId, status: "pass", details: {} })}\n`, stderr: "" }; if (argv.includes("--test-reporter=tap")) return { status: 0, stdout: approvalTap(), stderr: "" }; throw new Error(`unexpected execution command:${command}:${argv.join(" ")}`); }; }
function assertNoCommands(fn) { let calls = 0; assert.throws(() => fn(() => { calls += 1; return { status: 0, stdout: "", stderr: "" }; })); assert.equal(calls, 0); }

test("v26 exposes zero-argument formal APIs with no caller command injection and a /tmp-only harness", async () => { const value = await fixture(); assert.deepEqual(value.module.candidateV26(), { status: "PENDING_REVIEW", execution_authorized: false, container_create_authorized: false, container_execute_authorized: false, formal_go: false, docker_or_database_command_executed: false, trust_root: "repository-fixed-path-procedural-root-v1", review_chain: reviewChain }); assert.equal(value.rawModule.provisionV26.length, 0); assert.equal(value.rawModule.executeV26.length, 0); assert.match(String(value.rawModule.provisionV26), /provisionWithV26\(spawnSync\)/u); assert.match(String(value.rawModule.executeV26), /executeWithV26\(spawnSync\)/u); let injected = 0; chmodSync(pathOf(value, "candidate"), 0o644); assert.throws(() => value.rawModule.provisionV26({ runCommand: () => { injected += 1; } })); assert.throws(() => value.rawModule.executeV26({ runCommand: () => { injected += 1; } })); assert.equal(injected, 0); const live = await import(`${pathToFileURL(resolve(sourceRoot, runnerRelative)).href}?live-harness-rejection`); assert.throws(() => live.createIsolatedV26TestHarness(() => {}), /b2c-v26-test-harness-root/u); });

test("v26 uses one explicit code-point comparator for authorized and materialized path order", async () => {
  const value = await fixture();
  const paths = [
    "/snapshot/runtime-dependencies/a/node_modules/@scope/pkg/index.js",
    "/snapshot/runtime-dependencies/a/node_modules/.pnpm/pkg/index.js",
    "/snapshot/runtime-dependencies/a/node_modules/_private/index.js",
    "/snapshot/runtime-dependencies/a/node_modules/pkg/index.js",
    "/snapshot/runtime-dependencies/a/node_modules/@scope-pkg/index.js",
  ];
  const expected = [...paths].sort(value.module.compareCanonicalPathV26);
  assert.deepEqual([...paths].reverse().sort(value.module.compareCanonicalPathV26), expected);
  assert.deepEqual(expected, [...paths].sort());
  assert.notDeepEqual(expected, [...paths].sort((left, right) => left.localeCompare(right)));
});

test("v26 accepts the real registry pattern with shared historical run IDs while rejecting reuse of every target identity", async () => {
  const shared = await fixture(); const registry = JSON.parse(readFileSync(pathOf(shared, "registry"), "utf8")); assert.deepEqual(registry.resources.find(({ label }) => label === "v15"), failedV15); assert.deepEqual(registry.resources.find(({ label }) => label === "v17"), failedV17); assert.deepEqual(registry.resources.find(({ label }) => label === "v18"), failedV18); assert.deepEqual(registry.resources.find(({ label }) => label === "v19"), failedV19); assert.deepEqual(registry.resources.find(({ label }) => label === "v20"), failedV20); assert.deepEqual(registry.resources.find(({ label }) => label === "v21"), failedV21); assert.deepEqual(registry.resources.find(({ label }) => label === "v22"), failedV22); assert.deepEqual(registry.resources.find(({ label }) => label === "v23"), failedV23); assert.deepEqual(registry.resources.find(({ label }) => label === "v25"), failedV25); let acceptedCalls = 0; shared.module.provisionV26({ runCommand: (...args) => { acceptedCalls += 1; return provisionFake(shared)(...args); } }); assert.equal(acceptedCalls, 2);
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["database", ids.database], ["volume", "postgres:16-alpine"]]) { const value = await fixture({ registryMutator: (resources) => resources.map((entry, index) => index ? entry : { ...entry, [field]: target }) }); assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["container_id", ids.containerId], ["volume", ids.volumeId], ["volume", ids.imageId]]) { const value = await fixture({ migrations: true, registryMutator: (resources) => resources.map((entry, index) => index ? entry : { ...entry, [field]: target }) }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v15" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v17" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v18" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v19" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v20" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v21" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v22" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v23" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const [field, target] of [["run_id", ids.run], ["container", ids.container], ["container_id", ids.containerId], ["database", ids.database], ["volume", ids.volumeId]]) { const requiresExecution = ["container_id", "volume"].includes(field); const value = await fixture({ migrations: requiresExecution, registryMutator: (resources) => resources.map((entry) => entry.label === "v25" ? { ...entry, [field]: target } : entry) }); if (requiresExecution) { value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); } else assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const mutate of [(resources) => resources.map((entry, index) => index ? entry : { ...entry, container: "   " }), (resources) => resources.map((entry, index) => index ? entry : { ...entry, label: "Z" })]) { const value = await fixture({ registryMutator: mutate }); assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
});

test("v26 positive fake-command path proves four ordered injection points across three explicit catalog states", async () => {
  const value = await fixture({ migrations: true }); let provisionCalls = 0; value.module.provisionV26({ runCommand: (...args) => { provisionCalls += 1; return provisionFake(value)(...args); } }); assert.equal(provisionCalls, 2); const observation = JSON.parse(readFileSync(pathOf(value, "observation"), "utf8")); assert.equal(observation.container_id, ids.containerId); assert.equal(observation.image_id, ids.imageId); assert.deepEqual(observation.labels, { run_id: ids.run, provision_authority_sha256: value.hashes.provisionSha }); assert.deepEqual(observation.inherited_labels, { "desktop.docker.io/wsl-distro": "Ubuntu-26.04" }); assert.equal(lstatSync(pathOf(value, "observation")).mode & 0o777, 0o444);
  completeExecutionAuthority(value); const faultSqlInputs = [], orderedSqlInputs = []; let executionCalls = 0; const result = value.module.executeV26({ runCommand: (...args) => { executionCalls += 1; if (args[1]?.[0] === "exec") { assert.ok(args[1].includes("--tuples-only")); assert.ok(args[1].includes("--no-align")); } const input = String(args[2]?.input ?? ""); orderedSqlInputs.push(input); if (/v26-injected-/u.test(input)) faultSqlInputs.push(input); return executionFake(value)(...args); } }); assert.deepEqual(result.faults, [
    { boundary: "before-create", injection_point: "after-validation", catalog_state: "old-present-build-absent", marker: "v26-injected-before-create", sqlstate: "P0001", snapshot_exact: true },
    { boundary: "after-create", injection_point: "after-validation", catalog_state: "old-present-build-present", marker: "v26-injected-after-create", sqlstate: "P0001", snapshot_exact: true },
    { boundary: "after-drop", injection_point: "immediately-after-ddl", catalog_state: "old-absent-build-present", marker: "v26-injected-after-drop", sqlstate: "P0001", snapshot_exact: true },
    { boundary: "before-rename", injection_point: "after-validation", catalog_state: "old-absent-build-present", marker: "v26-injected-before-rename", sqlstate: "P0001", snapshot_exact: true },
  ]); assert.deepEqual(result.migration_000197, { filename: "000197_property_approval_active_source_index_forward_fix.sql", raw_sha256: "a9b98ca82aa4dafc16535085184df838880ef27801f7cd4b225e1ca1a15af059", status: "succeeded", dual_history_atomic: true, identical_byte_rerun: true, catalog_history_unchanged: true }); assert.equal(result.approval_port_pg.tests, 7); assert.equal(result.approval_port_pg.pass, 7); assert.equal(result.approval_port_pg.fail, 0); assert.deepEqual(result.approval_port_pg.names, approvalNames); assert.equal(result.approval_port_pg.fixture_cleanup, true); assert.equal(result.final_snapshot_exact, true); const migration184 = readFileSync(resolve(value.root, "database/migrations/000184_property_workbench_read_permissions.sql"), "utf8"), fixtureSql = readFileSync(resolve(value.root, fixtureRelative), "utf8"), migration185 = readFileSync(resolve(value.root, "database/migrations/000185_property_b_identity_schema_expand.sql"), "utf8"); assert.equal(orderedSqlInputs.filter((input) => input === fixtureSql).length, 1); assert.ok(orderedSqlInputs.indexOf(migration184) < orderedSqlInputs.indexOf(fixtureSql)); assert.ok(orderedSqlInputs.indexOf(fixtureSql) < orderedSqlInputs.indexOf(migration185)); assert.equal(sha(fixtureSql), "d23be07fe89347fa9e46ce08b3ddb64e62ad2e5d95670ad9408fe5af11e94523"); assert.equal(faultSqlInputs.length, 4); for (const input of faultSqlInputs.slice(1)) { assert.ok(input.includes("i.indisunique=true AND i.indisvalid=true AND i.indisready=true")); assert.ok(input.includes("array_agg(a.attname::text ORDER BY k.ordinal)")); assert.ok(input.includes("ARRAY['tenant_id','park_id','action_id','source_type','source_id','source_expected_version']::text[]")); assert.ok(input.includes("24ef911486d5274d6c439d63de6aa253b289241ac2b75317b1f98bc93a5a8fda")); assert.equal(input.includes("actual_index<>'dd004f0c2e5f40e86ec1953effa91b8604614e276c9fedabe7f2464f13d70d9c'"), false); } const afterDrop = faultSqlInputs[2], beforeRename = faultSqlInputs[3]; assert.ok(afterDrop.indexOf("v26-injected-after-drop") < afterDrop.indexOf("old index still present")); assert.ok(beforeRename.indexOf("old index still present") < beforeRename.indexOf("v26-injected-before-rename")); assert.ok(executionCalls > 200); const terminal = JSON.parse(readFileSync(resolve(value.research, `${prefix}-evidence-20260803/success-${ids.run}.json`), "utf8")); assert.deepEqual(Object.keys(terminal.authority_provenance).sort(), ["execution_authority_raw_sha256", "resource_observation_raw_sha256"]); assert.deepEqual(terminal.migration_000197, result.migration_000197); assert.deepEqual(terminal.approval_port_pg, result.approval_port_pg); assert.equal(terminal.runtime_dependency_manifest_raw_sha256, value.hashes.runtimeDependencySha); const evidenceRoot = resolve(value.research, `${prefix}-evidence-20260803`), manifestPath = resolve(evidenceRoot, `success-${ids.run}.manifest.json`), manifest = JSON.parse(readFileSync(manifestPath, "utf8")); assert.equal(manifest.status, "SUCCESS"); const independentlyEnumerated = recursiveFixtureFiles(evidenceRoot).filter((path) => path !== manifestPath).sort().map((path) => { const metadata = statSync(path); return { filename: path.slice(evidenceRoot.length + 1).replaceAll("\\", "/"), bytes: metadata.size, mode: (metadata.mode & 0o777).toString(8).padStart(4, "0"), raw_sha256: sha(readFileSync(path)) }; }); assert.deepEqual(manifest.files, independentlyEnumerated); assert.equal(new Set(manifest.files.map(({ filename }) => filename)).size, manifest.files.length); assert.ok(manifest.files.some(({ filename }) => filename === "formal-source-snapshot/apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts")); assert.ok(manifest.files.some(({ filename }) => filename === "formal-source-snapshot/node_modules/@jinhu/shared/dist/index.js")); assert.ok(manifest.files.some(({ filename }) => filename === "runtime-dependency-manifest.json")); assert.ok(manifest.files.every((entry) => entry.mode === "0444" && /^[0-9a-f]{64}$/u.test(entry.raw_sha256)));
});

test("v26 approval execution remains bound to the read-only snapshot after original source mutation", async () => {
  const value = await fixture({ migrations: true });
  value.module.provisionV26({ runCommand: provisionFake(value) });
  completeExecutionAuthority(value);
  const original = resolve(value.root, "apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts"), originalRaw = readFileSync(original), expectedSha = sha(originalRaw);
  const snapshotRoot = resolve(value.research, `${prefix}-evidence-20260803/formal-source-snapshot`), fake = executionFake(value);
  let mutated = false, approvalChildren = 0;
  const result = value.module.executeV26({ runCommand: (command, argv, options) => {
    if (command === value.runtimeExecutable) {
      if (!mutated) { writeFileSync(original, `${originalRaw.toString("utf8")}\n// post-snapshot mutation\n`); mutated = true; }
      approvalChildren += 1;
      assert.ok(argv.some((argument) => typeof argument === "string" && argument.startsWith(`${snapshotRoot}/apps/api/src/modules/property-approvals/`)));
      assert.equal(options.cwd, resolve(snapshotRoot, "apps/api"));
      assert.equal(options.env.TS_NODE_PROJECT, resolve(snapshotRoot, "apps/api/tsconfig.json"));
    }
    return fake(command, argv, options);
  } });
  assert.equal(mutated, true); assert.equal(approvalChildren, 5); assert.equal(result.approval_port_pg.pass, 7);
  const snapshotCli = resolve(snapshotRoot, "apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts");
  assert.equal(sha(readFileSync(snapshotCli)), expectedSha); assert.equal(lstatSync(snapshotCli).mode & 0o777, 0o444); assert.notEqual(sha(readFileSync(original)), expectedSha);
});

test("v26 approval children preload the snapshot resolver and reject live repository fallback", async () => {
  const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); const fake = executionFake(value); let checked = false;
  const result = value.module.executeV26({ runCommand: (command, argv, options) => { if (command === value.runtimeExecutable && !checked) { checked = true; const guard = argv[argv.indexOf("--require") + 1], secondRequire = argv.lastIndexOf("--require"), tsNode = argv[secondRequire + 1]; assert.ok(guard.startsWith(resolve(value.research, `${prefix}-evidence-20260803/formal-source-snapshot/`))); assert.ok(tsNode.includes("/formal-source-snapshot/runtime-dependencies/")); assert.equal(options.env.NODE_PATH, ""); const escaped = spawnSync(process.execPath, ["--require", guard, "--eval", "require('eslint')"], { cwd: options.cwd, env: { ...options.env, NODE_PATH: resolve(sourceRoot, "node_modules") }, encoding: "utf8" }); assert.notEqual(escaped.status, 0); assert.match(`${escaped.stdout}${escaped.stderr}`, /b2c-v26-runtime-resolution-escape/u); } return fake(command, argv, options); } });
  assert.equal(checked, true); assert.equal(result.approval_port_pg.pass, 7);
});

test("v26 rejects direct snapshot mutation before approval with zero approval children and a fixed failure terminal", async () => {
  const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value);
  const fake = executionFake(value), snapshotCli = resolve(value.research, `${prefix}-evidence-20260803/formal-source-snapshot/apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts`); let finalSnapshots = 0, approvalChildren = 0;
  assert.throws(() => value.module.executeV26({ runCommand: (command, argv, options) => { if (command === value.runtimeExecutable || (command === "/usr/bin/docker" && argv[2] === "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}")) approvalChildren += 1; const result = fake(command, argv, options); if (String(options?.input ?? "").includes("'old_predicate_matches'")) { finalSnapshots += 1; if (finalSnapshots === 2) { chmodSync(snapshotCli, 0o644); writeFileSync(snapshotCli, `${readFileSync(snapshotCli, "utf8")}\n// direct snapshot mutation\n`); } } return result; } }));
  assert.equal(approvalChildren, 0); const evidenceRoot = resolve(value.research, `${prefix}-evidence-20260803`), terminal = JSON.parse(readFileSync(resolve(evidenceRoot, `failure-${ids.run}.json`), "utf8")); assert.deepEqual(terminal.failure, { code: "B2C_V26_EXECUTION_FAILED", stage: "execution", name: "ExecutionError" }); assert.equal(JSON.parse(readFileSync(resolve(evidenceRoot, `failure-${ids.run}.manifest.json`), "utf8")).status, "FAILURE");
});

test("v26 rejects live runtime dependency mutation before approval with zero approval children", async () => {
  const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value);
  const fake = executionFake(value), dependency = resolve(value.root, "node_modules/typeorm/index.js"); let finalSnapshots = 0, approvalChildren = 0;
  assert.throws(() => value.module.executeV26({ runCommand: (command, argv, options) => { if (command === value.runtimeExecutable || (command === "/usr/bin/docker" && argv[2] === "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}")) approvalChildren += 1; const result = fake(command, argv, options); if (String(options?.input ?? "").includes("'old_predicate_matches'")) { finalSnapshots += 1; if (finalSnapshots === 2) writeFileSync(dependency, `${readFileSync(dependency, "utf8")}\n// runtime mutation\n`); } return result; } }));
  assert.equal(approvalChildren, 0); const evidenceRoot = resolve(value.research, `${prefix}-evidence-20260803`), terminal = JSON.parse(readFileSync(resolve(evidenceRoot, `failure-${ids.run}.json`), "utf8")); assert.deepEqual(terminal.failure, { code: "B2C_V26_EXECUTION_FAILED", stage: "execution", name: "ExecutionError" });
});

test("v26 binds exact runtime executable bytes and rejects executable drift before approval", async () => {
  const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); const fake = executionFake(value); let finalSnapshots = 0, approvalChildren = 0;
  assert.throws(() => value.module.executeV26({ runCommand: (command, argv, options) => { if (command === value.runtimeExecutable || (command === "/usr/bin/docker" && argv[2] === "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}")) approvalChildren += 1; const result = fake(command, argv, options); if (String(options?.input ?? "").includes("'old_predicate_matches'")) { finalSnapshots += 1; if (finalSnapshots === 2) { const metadata = statSync(value.runtimeExecutable); writeFileSync(value.runtimeExecutable, readFileSync(value.runtimeExecutable).map((byte, index) => index === 0 ? byte ^ 1 : byte)); chmodSync(value.runtimeExecutable, metadata.mode & 0o777); } } return result; } }));
  assert.equal(approvalChildren, 0); assert.equal(JSON.parse(readFileSync(resolve(value.research, `${prefix}-evidence-20260803/failure-${ids.run}.manifest.json`), "utf8")).status, "FAILURE");
});

test("v26 preserves fixed failure evidence when a snapshot file is replaced by a symlink", async () => {
  const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); const fake = executionFake(value), snapshotCli = resolve(value.research, `${prefix}-evidence-20260803/formal-source-snapshot/apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts`), original = resolve(value.root, "apps/api/src/modules/property-approvals/property-approval.port.pg-cli.ts"); let finalSnapshots = 0;
  assert.throws(() => value.module.executeV26({ runCommand: (command, argv, options) => { const result = fake(command, argv, options); if (String(options?.input ?? "").includes("'old_predicate_matches'")) { finalSnapshots += 1; if (finalSnapshots === 2) { chmodSync(dirname(snapshotCli), 0o755); unlinkSync(snapshotCli); symlinkSync(original, snapshotCli); } } return result; } }));
  const evidenceRoot = resolve(value.research, `${prefix}-evidence-20260803`), terminal = JSON.parse(readFileSync(resolve(evidenceRoot, `failure-${ids.run}.json`), "utf8")), manifest = JSON.parse(readFileSync(resolve(evidenceRoot, `failure-${ids.run}.manifest.json`), "utf8")); assert.deepEqual(terminal.failure, { code: "B2C_V26_EXECUTION_FAILED", stage: "execution", name: "ExecutionError" }); assert.equal(manifest.status, "FAILURE"); assert.ok(manifest.files.some((entry) => entry.type === "symlink" && entry.raw_sha256 === null), JSON.stringify(manifest.files.filter((entry) => entry.filename.includes("property-approval.port.pg-cli"))));
});

test("v26 rejects every canonical intake path when missing, mutable, or symlinked before creation", async () => {
  for (const key of ["candidate", "manifest", "registry", "staticRecord", "node22Tap", "node24Tap", "databaseReview", "qaReview", "securityReview", "drainReview", "provisionAuthority"]) {
    for (const condition of ["missing", "mutable", "symlink"]) { const value = await fixture(); const path = pathOf(value, key); if (condition === "missing") renameSync(path, `${path}.elsewhere`); else if (condition === "mutable") chmodSync(path, 0o644); else { renameSync(path, `${path}.target`); symlinkSync(`${path}.target`, path); } assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  }
});

test("v26 rejects exact schema, field, decision, numeric-zero, hash, registry, resource, and live-byte drift before creation", async () => {
  const cases = [
    ["candidate", (v) => ({ ...v, extra: true })], ["candidate", (v) => { delete v.status; return v; }], ["candidate", (v) => ({ ...v, formal_go: "false" })], ["manifest", (v) => ({ ...v, review_chain: [...v.review_chain].reverse() })], ["databaseReview", (v) => ({ ...v, decision: "NO-GO" })], ["qaReview", (v) => ({ ...v, open_p0: "0" })], ["securityReview", (v) => ({ ...v, review_approved: false })], ["drainReview", (v) => ({ ...v, candidate_authority_raw_sha256: "0".repeat(64) })], ["provisionAuthority", (v) => ({ ...v, anonymous_volume: false })], ["provisionAuthority", (v) => ({ ...v, container_execute_authorized: true })], ["provisionAuthority", (v) => ({ ...v, image_reference: "postgres:latest" })], ["registry", (v) => ({ ...v, extra: true })], ["registry", (v) => ({ ...v, resources: v.resources.slice(1) })], ["registry", (v) => ({ ...v, resources: v.resources.map((entry, index) => index ? entry : { ...entry, run_id: ids.run }) })],
  ];
  for (const [key, mutate] of cases) { const value = await fixture(); edit(pathOf(value, key), mutate); assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  { const value = await fixture(); const path = pathOf(value, "provisionAuthority"); const raw = readFileSync(path, "utf8").replace('"host_port_bindings": 0', '"host_port_bindings": -0'); seal(path, raw); assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
  for (const live of [runnerRelative, specRelative]) { const value = await fixture(); const path = resolve(value.root, live); chmodSync(path, 0o644); writeFileSync(path, `${readFileSync(path, "utf8")}\n`); chmodSync(path, 0o444); assertNoCommands((runCommand) => value.module.provisionV26({ runCommand })); }
});

test("v26 failure receipts use fixed fields, omit secret error text, and reject extra inspect labels", async () => { const secret = "TOP-SECRET-json-or-child-error"; const value = await fixture(); assert.throws(() => value.module.provisionV26({ runCommand: provisionFake(value, { failInspect: true, secret }) })); const receiptRaw = readFileSync(pathOf(value, "observation"), "utf8"), receipt = JSON.parse(receiptRaw); assert.equal(receipt.status, "PROVISION_FAILED"); assert.equal(receipt.run_id_reusable, false); assert.equal(receipt.cleanup_attempted, false); assert.deepEqual(receipt.failure, { code: "B2C_V26_PROVISION_OBSERVATION_FAILED", stage: "post-create-observation", name: "ProvisionObservationError" }); assert.equal(receiptRaw.includes(secret), false); assert.equal(lstatSync(pathOf(value, "observation")).mode & 0o777, 0o444);
  const extra = await fixture(); let calls = 0; assert.throws(() => extra.module.provisionV26({ runCommand: (...args) => { calls += 1; return provisionFake(extra, { extraLabels: { attacker: "label" } })(...args); } })); assert.equal(calls, 2); const extraRaw = readFileSync(pathOf(extra, "observation"), "utf8"); assert.equal(extraRaw.includes("attacker"), false); assert.equal(JSON.parse(extraRaw).status, "PROVISION_FAILED");
  const prefix = await fixture(); assert.throws(() => prefix.module.provisionV26({ runCommand: provisionFake(prefix, { image: ids.imageId }) })); assert.equal(JSON.parse(readFileSync(pathOf(prefix, "observation"), "utf8")).status, "PROVISION_FAILED");
});

test("v26 rejects extra observed labels before execute and extra live labels into a fixed failure terminal", async () => {
  const intake = await fixture({ migrations: true }); intake.module.provisionV26({ runCommand: provisionFake(intake) }); edit(pathOf(intake, "observation"), (value) => ({ ...value, labels: { ...value.labels, attacker: "secret-label" } })); completeExecutionAuthority(intake); assertNoCommands((runCommand) => intake.module.executeV26({ runCommand }));
  const live = await fixture({ migrations: true }); live.module.provisionV26({ runCommand: provisionFake(live) }); completeExecutionAuthority(live); let calls = 0; assert.throws(() => live.module.executeV26({ runCommand: (...args) => { calls += 1; return executionFake(live, { extraLabels: { attacker: "secret-label" } })(...args); } })); assert.equal(calls, 1); const evidence = resolve(live.research, `${prefix}-evidence-20260803`); const combined = readdirSync(evidence).map((name) => readFileSync(resolve(evidence, name), "utf8")).join("\n"); assert.equal(combined.includes("secret-label"), false); const terminal = JSON.parse(readFileSync(resolve(evidence, `failure-${ids.run}.json`), "utf8")); assert.deepEqual(terminal.failure, { code: "B2C_V26_EXECUTION_FAILED", stage: "execution", name: "ExecutionError" });
  const drift = await fixture({ migrations: true }); drift.module.provisionV26({ runCommand: provisionFake(drift) }); completeExecutionAuthority(drift); let driftCalls = 0; assert.throws(() => drift.module.executeV26({ runCommand: (...args) => { driftCalls += 1; return executionFake(drift, { extraLabels: { "desktop.docker.io/wsl-distro": "Ubuntu-24.04" } })(...args); } })); assert.equal(driftCalls, 1);
});

test("v26 execute rejects observation/execution authority schemas, bindings, modes, symlinks, migration drift, and fixture drift with zero child commands", async () => {
  const mutations = [["observation", (v) => ({ ...v, extra: true })], ["observation", (v) => ({ ...v, container_id: "d".repeat(64) })], ["observation", (v) => ({ ...v, host_port_bindings: "0" })], ["executionAuthority", (v) => ({ ...v, container_create_authorized: true })], ["executionAuthority", (v) => ({ ...v, formal_go: true })], ["executionAuthority", (v) => ({ ...v, resource_observation_raw_sha256: "0".repeat(64) })]];
  for (const [key, mutate] of mutations) { const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); edit(pathOf(value, key), mutate); assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); }
  for (const key of ["observation", "executionAuthority"]) for (const condition of ["mutable", "symlink"]) { const value = await fixture({ migrations: true }); value.module.provisionV26({ runCommand: provisionFake(value) }); completeExecutionAuthority(value); const path = pathOf(value, key); if (condition === "mutable") chmodSync(path, 0o644); else { renameSync(path, `${path}.target`); symlinkSync(`${path}.target`, path); } assertNoCommands((runCommand) => value.module.executeV26({ runCommand })); }
  const drift = await fixture({ migrations: true }); drift.module.provisionV26({ runCommand: provisionFake(drift) }); completeExecutionAuthority(drift); const migration = resolve(drift.root, "database/migrations/000185_property_b_identity_schema_expand.sql"); writeFileSync(migration, `${readFileSync(migration, "utf8")}\n`); assertNoCommands((runCommand) => drift.module.executeV26({ runCommand }));
  const fixtureDrift = await fixture({ migrations: true }); fixtureDrift.module.provisionV26({ runCommand: provisionFake(fixtureDrift) }); completeExecutionAuthority(fixtureDrift); const fixtureFile = resolve(fixtureDrift.root, fixtureRelative); chmodSync(fixtureFile, 0o644); writeFileSync(fixtureFile, `${readFileSync(fixtureFile, "utf8")}\n`); assertNoCommands((runCommand) => fixtureDrift.module.executeV26({ runCommand }));
  const fixtureMissing = await fixture({ migrations: true }); fixtureMissing.module.provisionV26({ runCommand: provisionFake(fixtureMissing) }); completeExecutionAuthority(fixtureMissing); unlinkSync(resolve(fixtureMissing.root, fixtureRelative)); assertNoCommands((runCommand) => fixtureMissing.module.executeV26({ runCommand }));
});

test("v26 snapshot and full P0001 marker contracts accept the exact verbose psql prefix while rejecting whitespace drift, ambiguity, and substrings", async () => { const value = await fixture({ migrations: true }); const snapshot = exactSnapshot(value); assert.doesNotThrow(() => value.module.assertSnapshotV26(snapshot)); for (const changed of [{ ...snapshot, extra: true }, { ...snapshot, forbidden_primary: "0" }, { ...snapshot, approval_rows: -0 }, { ...snapshot, catalog: { ...snapshot.catalog, extra: false } }, { ...snapshot, primary_history: snapshot.primary_history.map((entry, index) => index ? entry : { ...entry, extra: true }) }]) assert.throws(() => value.module.assertSnapshotV26(changed)); for (const output of ["ERROR:  P0001: v26-injected-after-drop\n", "psql:<stdin>:1: ERROR:  P0001: v26-injected-after-drop\nCONTEXT: PL/pgSQL function inline_code_block line 1 at RAISE\nLOCATION: exec_stmt_raise, pl_exec.c:3905\n"]) assert.doesNotThrow(() => value.module.assertFaultMarkerV26(output, "v26-injected-after-drop")); for (const output of ["ERROR: P0001: prefix-v26-injected-after-drop\n", "ERROR: P0001: v26-injected-after-drop-suffix\n", "ERROR: P0001: v26-injected-after-drop\nERROR: P0001: other\n", "psql:other.sql:1: ERROR: P0001: v26-injected-after-drop\n", "psql:<stdin>:1:\tERROR: P0001: v26-injected-after-drop\n", "psql:<stdin>:1:  ERROR: P0001: v26-injected-after-drop\n"]) assert.throws(() => value.module.assertFaultMarkerV26(output, "v26-injected-after-drop")); });
