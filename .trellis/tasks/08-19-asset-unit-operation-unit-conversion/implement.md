# 实施计划

## 1. Schema 与实体

- [x] 选择新的无冲突迁移编号，新增楼栋/楼层映射列、索引、数据库父子链防线和不可变审计表。
- [x] 更新 Building/Floor 实体，不修改旧迁移。
- [x] 增加 schema 契约测试和 PostgreSQL 层级归属/不可变测试。

## 2. 共享映射服务

- [x] 在资产/共享物业边界实现 AssetSpaceMappingService，统一拥有锁、范围、唯一性、父子归属和审计写入。
- [x] 实现楼栋、楼层的创建/关联及房号受控转换。
- [x] 所有资产来源 decimal 字段使用字符串投影到持久化实体，不经过额外浮点计算。
- [x] 收口 PropertyOperationsService 的旧绑定与解绑路径到共享服务。

## 3. API、DTO 与权限

- [x] 新增三个资产转换/映射端点及 DTO，使用幂等拦截器、现有粒度权限和 AuditLog。
- [x] 对关联已有对象与新建对象使用明确 discriminator，服务端对每个变体重验。
- [x] 添加服务、控制器权限与幂等契约测试。

## 4. Web 最小入口

- [x] 在房源管理页增加物理资产候选与运营映射状态、“从资产启用”操作。
- [x] 级联依赖未完成时显示明确引导，不自动在背景创建父级。
- [ ] 桌面和 390px 视口实际验收（本地浏览器被登录态阻断）。

## 5. 验证与回滚

- [x] API 单元测试、PostgreSQL 并发集成测试通过。
- [ ] API/Web lint/typecheck/build 通过（API build 通过；Web build 被既有缺失页面阻断）。
- [ ] 重跑只读基线工具，新写入样本无范围/父子归属异常。
- [ ] 上线前备份；迁移失败停止发布。应用回滚保留新列和不可变审计，不执行破坏性 down migration。

## 风险点

- 楼栋/楼层两套编码和面积语义不完全一致，不得在没有映射确认的情况下自动合并。
- 当前工作区有公寓未提交变更，本任务不修改公寓文件。
- 新迁移需要避开现有重复 `000136_*` 历史和当前未提交的 `000214`。
