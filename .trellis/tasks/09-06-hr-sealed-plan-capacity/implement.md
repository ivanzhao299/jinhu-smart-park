# Execution

One implement agent owns executor constant/load-site, existing entrypoint contract tests and docs/yuzhou-hr-production-import-entrypoint.md. Root owns task/evidence notes, one local opt-in large-read run and remote CI state. Preserve other edits and all worktrees. No full A/B, DB writes, private data or new dependency installation.

Run standard entrypoint contracts and syntax/diff validation. Root runs the optional65MiB probe once after implementation and capacity check. Independent check agent reviews full diff and runs targeted lint/tests. Integrate with current T3 candidate branch to avoid a separate repeated full CI for a two-line executor fix. Do not claim entire production import or goal complete.
