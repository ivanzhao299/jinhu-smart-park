// Compatibility surface for existing Property/Party consumers. Re-exporting the
// same class preserves the Nest provider token and ciphertext behavior exactly.
export { PartySensitiveDataService } from "../../shared/security/party-sensitive-data.service";
