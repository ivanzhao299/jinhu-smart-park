# 共享房产底座实施计划

## 1. 实施步骤

- [ ] 确认 `asset_*` 与 `biz_*` 当前数据映射和 API 使用面。
- [ ] 定义共享枚举、响应类型和权限常量。
- [ ] 设计新的前向迁移编号，避开历史重复 `000136`。
- [ ] 实现经营配置和模式切换日志。
- [ ] 实现统一占用账本、事务和冲突语义。
- [ ] 实现个人业务相对方及敏感字段策略。
- [ ] 为现有商业合同增加占用兼容适配。
- [ ] 增加 API、权限、菜单/模块声明和审计。
- [ ] 增加单元测试、并发测试和目标 E2E。
- [ ] 更新领域蓝图、迁移说明和 UAT 矩阵。

## 2. 风险文件

- `apps/api/src/modules/units/**`
- `apps/api/src/modules/assets/**`
- `apps/api/src/modules/leasing-contracts/**`
- `packages/shared/src/index.ts`
- `database/migrations/**`
- production seed 和权限基线

## 3. 回滚

- 使用新迁移和独立表，避免破坏现有字段。
- 新能力由模块授权和功能开关控制。
- 初期商业合同使用读取适配器，可在不迁移历史数据的情况下回退。
- 不删除现有 `asset_*` 或 `biz_*` 数据。

## 4. 验证命令

- API 单元测试和目标模块 typecheck/lint/build。
- 新迁移在全新库和已有 UAT 快照副本上验证。
- 并发占用专项脚本。
- 现有资产、合同、应收和全量回归。
