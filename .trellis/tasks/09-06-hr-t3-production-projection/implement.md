# Execution

1. One implementer owns new pure T3 projection module and minimal shared identity exports in existing T3 phase producer. Root owns synthetic tests, package/spec/docs and reads real safe source metadata when useful.
2. Implement every eight-table field projection and source/dependency identities, exact decimals, days and explicit semantic failure records.
3. Root tests all target fields and identity parity with existing phase producer; empty/unknown/null/duplicate/invalid/precision cases and immutable input. Proportionate local literal-only PostgreSQL checks, not business writes.
4. Independent Trellis reviewer checks all changed files and regression scope. Fresh fetch and isolated PR path after verification; previous PR638/639 and other worktrees remain untouched.
5. Next necessary slice is source/target/T0-bound private T3 candidate assembly and final freeze integration. Goal remains full production import plus business/API/UI parity, not this helper alone.
