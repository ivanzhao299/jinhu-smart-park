import { createHash } from "node:crypto";
import { canonicalHash } from "../hr-cutover/materialize-reviewed-job-state.mjs";
import { canonicalDecisionHash, canonicalEvidenceIndexHash } from "../hr-cutover/yuzhou-job-state-decision-artifact-lib.mjs";
import { compileYuzhouJobStateMachineAttestation, computeYuzhouJobStateCheckpointArtifactHash, computeYuzhouJobStateCheckpointRoot } from "../hr-cutover/yuzhou-job-state-machine-attestation.mjs";

export const digest = value => createHash("sha256").update(value).digest("hex");
export const h = value => value.repeat(64);

export function buildJobStateV2Fixture({ expectedDatabaseItemsSha256 = h("c") } = {}) {
  const trustedRootSha256 = h("9"), triple = { codeSha: "1".repeat(40), sourceSnapshotHash: h("2"), mappingContractHash: h("3") }, counts = [401, 402, 403, 404, 405, 406, 528], targets = ["active", "probation", "suspended", "departed"];
  const decisions = counts.map((observedRecordCount, index) => ({ sourceIdentitySha256: digest(`identity-${index}`), sourceRowSha256: digest(`row-${index}`), observedRecordCount, decision: index < 4 ? "map" : "quarantine", targetEmploymentStatus: index < 4 ? targets[index] : null, semanticClassification: index < 4 ? "derived_deterministic" : "quarantined_ambiguous", reasonCode: index < 4 ? "DETERMINISTIC_MAPPING" : ["AMBIGUOUS_SEMANTICS", "UNKNOWN_SOURCE_VALUE", "CONFLICTING_SOURCE_EVIDENCE"][index - 4] })).sort((a, b) => a.sourceIdentitySha256.localeCompare(b.sourceIdentitySha256));
  const evidenceIndex = { checkpointSha256: trustedRootSha256, manifestSha256: h("4"), extractBindingSha256: h("5"), journalSha256: h("6"), employeeJobStatesSha256: h("7"), jobStateCodeMetadataSha256: h("8"), jobStateCodesSha256: h("a") };
  const decision = { formatVersion: 2, artifactKind: "yuzhou_employee_job_state_machine_decision", artifactVersion: "v2", artifactStatus: "MACHINE_CANDIDATE", triple, expectedCheckpointRootSha256: trustedRootSha256, checkpointRootSha256: trustedRootSha256, evidenceIndex, evidenceIndexSha256: canonicalEvidenceIndexHash(evidenceIndex), scopeBinding: { tenantIdentitySha256: digest("tenant\0x"), parkIdentitySha256: digest("park\0x") }, sourceContract: { sourceSystem: "yuzhou-v10", dictionaryCode: "employee_job_state", sourceSnapshotSha256: h("b"), sourceDistinctStateCount: 7, sourceRecordCount: 2949 }, decisions,
    semanticLedger: { sourceDistinctStateCount: 7, sourceRecordCount: 2949, mappedStateCount: 4, quarantinedStateCount: 3, mappedRecordCount: decisions.filter(row => row.decision === "map").reduce((sum, row) => sum + row.observedRecordCount, 0), quarantinedRecordCount: decisions.filter(row => row.decision === "quarantine").reduce((sum, row) => sum + row.observedRecordCount, 0), conservationVerified: true }, canonicalDecisionSha256: "", machineAssertion: { mode: "trusted_root_deterministic_machine_semantics", policyVersion: "yuzhou-job-state-machine-policy-v2", status: "PASS", reasonCodes: [], humanSignature: false, humanIdentityAsserted: false }, productionImport: "HOLD" };
  decision.canonicalDecisionSha256 = canonicalDecisionHash(decision);
  const payload = { formatVersion: 2, kind: "yuzhou-job-state-private-materialization", canonicalDecisionSha256: decision.canonicalDecisionSha256, dictionaryVersionId: "00000000-0000-4000-8000-000000000099", expectedDatabaseItemsSha256, csm: triple, t0Binding: { manifestSha256: evidenceIndex.manifestSha256, employeeJobStatesSha256: evidenceIndex.employeeJobStatesSha256, jobStateCodeMetadataSha256: evidenceIndex.jobStateCodeMetadataSha256, jobStateCodesSha256: evidenceIndex.jobStateCodesSha256 }, scope: { tenantId: "10000001", parkId: "20000001", ...decision.scopeBinding }, dictionaryEvidenceSha256: decision.sourceContract.sourceSnapshotSha256, machineActor: { id: "00000000-0000-4000-8000-000000000101", kind: "machine_policy_engine", verifiedAt: "2026-08-29T00:00:00Z" }, items: decisions.map((row, index) => ({ id: `00000000-0000-4000-8000-${String(index + 1).padStart(12, "0")}`, sourceCode: `S-${index + 1}`, sourceName: `State ${index + 1}`, sourceValue: null, sourceIdentitySha256: row.sourceIdentitySha256, sourceRowSha256: row.sourceRowSha256 })), productionImport: "HOLD" };
  payload.payloadSha256 = canonicalHash(payload);
  const t0Evidence = { ...payload.t0Binding, dictionaryEvidenceSha256: payload.dictionaryEvidenceSha256, sourceDistinctStateCount: 7, sourceRecordCount: 2949 };
  const checkpoint = { formatVersion: 2, kind: "yuzhou-job-state-preload-package", trustedCheckpointRootSha256: trustedRootSha256, triple, decisionArtifact: decision, privatePayload: payload, t0Evidence, bindings: { decisionArtifactSha256: computeYuzhouJobStateCheckpointArtifactHash("decision_artifact", decision), privatePayloadArtifactSha256: computeYuzhouJobStateCheckpointArtifactHash("private_payload", payload), t0EvidenceArtifactSha256: computeYuzhouJobStateCheckpointArtifactHash("t0_evidence", t0Evidence) }, packageRootSha256: "" };
  checkpoint.packageRootSha256 = computeYuzhouJobStateCheckpointRoot(checkpoint);
  const attestation = compileYuzhouJobStateMachineAttestation(checkpoint, { expectedCheckpointRootSha256: trustedRootSha256 });
  const config = { runId: "yzfull-20260829T000000Z-11111111-rA", rehearsal: "A", triple, machineAttestation: { checkpointVersion: 2, trustedRootSha256 } };
  return { decision, payload, attestation, checkpoint, config, trustedRootSha256 };
}
