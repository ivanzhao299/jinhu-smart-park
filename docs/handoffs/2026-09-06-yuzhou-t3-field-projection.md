# T3 考勤和社保生产字段转换：已验证事实

本切片补上八类目标表的字段转换层，尚未生成最终生产写入计划，没有写入生产业务数据。它不代表玉舟全部功能复现完成。

## 实际源数据核对

受控 source manifest 字节 SHA 为 `f73e1caa87068b49c3679f2ce80f152046e524a3f7059e89c5cfda48f6f81b03`。在私有环境对三个暂存文件进行了只读流式校验，实际文件 SHA 与清单一致，读取前后文件身份、大小和时间未变。公开材料只保留聚合事实。

| 来源及拆分 | 数量 |
| --- | ---: |
| 日历父记录 | 144 |
| 每日考勤记录 | 4,383 |
| 保险政策父记录 | 12 |
| 政策项目 | 144 |
| 员工保险期间 | 35,008 |
| 员工保险项目 | 210,048 |
| 导入批次 | 1 |
| 观察到的符号规则 | 5 |
| 总投影 | 249,745 |
| 字段可表达投影 | 249,563 |
| 明确待补证或审查投影 | 182 |

守恒：249,745 = 249,563 + 182；不是“249,563 条已批准导入”。

实际待处理项：

- 144 个政策项目采用旧六字段布局，缺少四种固定金额字段。识别该布局并保留身份，但不伪填空值或套用新版费率单位；标记 `T3_POLICY_FIXED_AMOUNTS_UNATTESTED`。
- 5 条员工保险父记录存在目标整数表达问题，连同其 30 条子记录计 35 个 `T3_INT4_INVALID` 投影。没有修改来源或猜测有效年月。
- 3 个符号规则未建立业务映射。对应每日记录保留原符号并 `needs_review`，不伪造规则。
- 210,048 个非空原始旗标和 99,816 个负基数标记保留。旗标不直接解释为参保或停保；35,003 条可表达期间保留 `needs_review`，其余 5 条期间已计入整数审查项。

本次流式投影摘要 SHA：`9afc6f6939dc7aa238c3d5f772ed39c4dee293fc2d8b2e47d469e041183fcea1`。这是当前本地代码的准备层验证，不是已合并代码、生产运行或最终候选回执。原始行 SHA 引自已绑定暂存链，未从降维字段伪造重算。

## 实现与验证

- 新增纯 T3 字段转换模块；既有阶段清单提取共享身份公式，并精确接纳已证实的旧六字段政策布局。身份算法及其他校验不变，部分缺字段仍拒绝。
- 三种金额精度分别为六位、三位、两位，精确字符串转换，不浮点舍入、不重复除以 100。
- 11 项契约测试通过，包含实际本地 PostgreSQL 只读字面量转换；既有阶段清单契约通过。
- 合成 48 条投影与原阶段生成器的来源身份完全一致；旧布局另有 12 子项守恒及缺字段负例。
- 没有全量 A/B、数据库重建、完整工资提取、生产写入、附件读取或前端改动。

## 下一项必要工作

本轮进一步读历史转换代码，证实旧 variant=2 实际保留原 suffix_2 固定加数，而不是第二套比例。对现有 12 条政策只读重建全部 51 个原字段，12/12 原始行 SHA 完全吻合；调用当前已验证规范化函数可得到 72 个政策项目。未修改暂存文件、未重新提取。规范化摘要 SHA：`0d7b1577ffab8ea118202cdb95f74155d555a8fba5f660d97d12472d28dc140d`。这项恢复证明尚未接入本层投影，所以前述 144 个待补证状态仍如实保留，不能宣称已形成生产计划。

下一项是把该恢复证明做成可复用的源行哈希绑定适配器，明确旧 144 子项到新 72 项的对应关系，再接入源/目标/T0 依赖绑定的流式候选组装。无需扩大到完整源库重新提取。之后通过既有冻结层生成当前代码身份的计划。最终还需当前目标冲突、备份、授权和执行质量核对；前端/API/存储过程业务等价必须独立验收，不能用本切片计数替代。

## 证据引用修复

更新政策字段契约中阶段生成器的字节哈希，同时检查发现两个主线已有的过期引用：payload 测试仅增加孤儿记录隔离断言，HR service 仅将敏感数据服务 import 移到共享目录，保险语义未变。逐项审阅对应差异后仅重绑这两个引用及冻结清单的父哈希，不增加兼容分数，也不替换业务/生产验收。
