## 2026-09-10 已批准审核决定（优先于下方历史规划）

主助手已实际复核报告、CSS/Parts 和 primary-390.png，批准 G1+G2：D01/D02/D03/D05/D06。保留 native dialog/showModal、closed 隐藏、锁/幂等指纹/API/payload/权限/审批和金融语义；原因 trim 2–500，民宿未到店/遗失 busy/error 接入，占用释放局部错误+false。
G3 不批准：保留取消清空原因契约，失败保留有效输入，取消不重置 caller 原表单。G4/G5 不批准额外确认或 leasing 批量迁移，登记为非违规建议，不算漏项。D11 必做代表回归，仅证实与本共享改动相关回归时最小修复留证；D12 不改。
授权激活、正常分支实现、Issue/PR、必要 PR CI/Release Smoke 全通过后 squash merge；核验精确 main SHA 的 CI/自动 Deploy 与清理后归档。禁止豁免、force push、手动生产操作或修改他人环境。
验收必须分开 mock 前端与隔离真实 API/DB：原路径唯一申请回读、执行前配置未变，以及修改 caller 代表失败/成功；1440/390、焦点/Escape/busy/回焦、短屏/200% 重排、复杂长内容及可构造错误分级。必要验收受阻保持未闭环。
Issue: https://github.com/ivanzhao299/jinhu-smart-park/issues/732
基线核对：HEAD = fetched origin/main = 563af3a1890b89166f664dd5427e47267ae90b7e；本 worktree 仅既有专项未跟踪工件；COST_GUARD 延续，未重扫145文件。

# 弹窗与审批交互规范专项

状态：planning，调查交付待主助手复核。用户已授权建任务及复核后修复闭环，但本轮仅调查方案，禁止自行转实施。

## 用户目标
房源经营配置→详情→申请经营模式切换→提交审批的弹窗符合既有系统规范，并排查民宿、长租同类问题，减少输入丢失、反馈不可见和确认误操作。

## 已确认事实
- 基线 origin/main：563af3a1890b89166f664dd5427e47267ae90b7e；原工作区干净，在独立 worktree 调查。
- 原路径是 ConsequenceDialog / native dialog；不是 window.prompt/confirm。审批策略由后端冻结，前端没有配置/预览层。
- 本轮真实路由 mock API 浏览器在1440/390复现框体左上且无内边距；民宿未到店漏忙态/错误；原因上限缺失。
- 既有关闭清空原因是测试规定的契约，保留输入需复核；housing作废的错误/忙态接入正确。
- 13项定向状态/源码测试通过，不构成视觉通过。无真实后端/数据库验收。

## 要求
1. 全部发现可溯源：路由、操作、固定SHA文件行、具体规范、可复用能力和风险；推测与确认分列。
2. 修复优先复用已有组件；约束覆盖表单校验、取消留稿、busy/防重复、错误可见、对象和状态摘要、键盘焦点、390px、层叠滚动。
3. 不修改API/权限/审批状态机/金融语义，不以静态测试替代实际浏览器。
4. 评估所有原生控件的用途，不对普通表单控件或离页保护一概判违规。

## 调查验收（本轮）
- [x] 固定SHA，保护原工作区/其他worktree与活跃进程。
- [x] 规范表、全调用链、一次范围扫描、发现矩阵。
- [x] 原路径与民宿/长租代表真实页面的桌面和390px交互，明确mock边界。
- [x] report、design.md、implement.md及research证据。
- [ ] 主助手复核和具体修复分组批准（下一阶段门槛）。

## 修复后验收（待实施）
- G1/G2所有明确缺陷闭环，相关共用消费者无回归。
- 复核同意的留稿/确认流程全部通过取消、错误、重试、换目标测试。
- 桌面/390px真实交互和必要真实后端证明，不修改业务不变量。

## 非目标
HR、生产、部署、他人容器/主Chrome、全仓弹窗重写、API新增/审批预览虚构、金融状态改动、CI豁免、自动提交/合并。

## 待决
G3默认与opt-in清稿策略；G4/G5扩大范围；初始焦点/长内容操作区方案；独立真实后端验收环境。详见审查报告第8节。
