# PAM-004 + PAM-005 修复复测 UAT 第三轮

## 结论

- RUN_ID：`20260827-220612`
- 被测 revision：`f15869d0596046d893498a9bf0941a222a6dece7`（执行前 `origin/main`）
- branch：`codex/pam-fix3-uat-evidence-20260827-220612`
- 执行者：`emvia`（Codex CLI）
- PostgreSQL / API / Web / CDP：`56537 / 3261 / 3262 / 9571`
- Chrome：`151.0.7922.138`，CDP protocol `1.3`，viewport `1440×960`
- 结论：**BLOCKED（5 PASS / 1 browser harness evidence blocker）/ 不归档**

Phase 0、发布初始化、curl 热身、真实 Chrome login selector 预检和六项 fixture 建链均完成。五项业务 Case 取得真实浏览器 PASS 证据；`PAM-005-PARK-SWITCH` 的 runner 等待条件在园区切换完成前即满足，截图仍显示 Park A，不能把该次结果计为 PASS。依同题最多两次约束停止，不做第三次。

## Phase 0 与环境门禁

Chrome 启动前已在 local-only 根 `/tmp/jinhu-pam-fix3-20260827-220612/preflight/` 完成三工件并 SHA-256 留档：

| 工件 | SHA-256 |
| --- | --- |
| `design-basis.md` | `c2185a59a5f4169e81d311b7317d457715a749b07f316abcc45538888c19da2c` |
| `design-implementation-audit.md` | `a887b4cb607e6dab86e7421ccefdba55c1ccae73d9235f52aa183aaadd6ad7fb` |
| `role-flow-matrix.md` | `0264976871d1c97d25944911794b85ab0ad44b796280a4b6b6a87b5d47936376` |

- 独占 compose project：`jinhu-pam-fix3-uat-20260827-220612`；启动前候选端口均空闲，未触碰其他容器或主 Chrome。
- migration：264 条 `succeeded`；production-safe seed 成功；bootstrap 后 strict baseline PASS。
- 首次 migration 在 PostgreSQL 尚未 ready 时失败，readiness PASS 后重试成功；失败日志保留为 `migrate.log`，成功日志为 `migrate-retry1.log`。
- 运行中发现 frozen env 数据库名被 harness 的机械替换改变。停止 API/Web 后，仅对本轮独占数据库执行精确 rename，重启后验证 `current_database` 与 264 条历史一致；未跨环境写入。
- `/api/v1/ready=200`，`/login=200`；真实 Chrome login 预检 selector、标题、viewport 均 PASS。

## Fixture 策略

实际采用：**API fixture 例外**。

修正后的 UI harness 两次都在管理员无 scope UI 登录处收到 `401`，未进入园区/角色/用户写入。产品 API 的 scoped admin login 同时为 `200`，说明 blocker 位于该 UI fixture 登录方式，而非 API/数据库不可用。两次失败路径均落盘 Chrome Network target；UI fixture teardown 均记录 `/login → about:blank → Browser.close → profile 删除`。

随后按预授权，仅通过产品自身管理端点完成 fixture：

- `GET /roles|/parks|/users?page=1&page_size=100&keyword=...` 先查后建；
- `POST /parks` 创建 Park B；
- `POST /roles`、`POST /roles/:id/permissions` 创建 page/action 角色并授权；
- `POST /roles/:id/copy` 从 `HOUSING_OPERATOR` 实例化 Track-B；
- `POST /users`、`POST /users/:id/roles` 创建并分配四个业务账号；
- 共 14 次产品 API 写、11 次产品 API 读；无 SQL fixture 直插。

Park B 管理员扩展中曾错误先更新管理员默认园区，触发 `auth_version` 轮换。使用仓库受支持的 `ALLOW_PASSWORD_RESET=yes pnpm db:bootstrap:admin` 在默认园区恢复首管；未用 SQL 修复 fixture。该过程及失败 Network 均保留。

## 六项矩阵

| Case | 结果 | 浏览器断言与证据 |
| --- | --- | --- |
| PAM-004-DISABLED | PASS | API menu/Sidebar 均无 housing；`/housing` 显示“你没有查看此页面内容的权限”。`01`、`02` 截图 |
| PAM-005-ENABLED-LANDING | PASS | 启用后 `/housing/dashboard` 同时存在于 API tree、Sidebar，landing 属于 Sidebar。`03` 截图 |
| ACTION-ONLY | PASS | 仅 `housing:lease:create`；允许通用 `/dashboard`，无 `/housing*` 业务 surface。`04` 截图 |
| TRACK-B-TASK-DESK-ONLY | PASS | API/Sidebar housing href 精确为 `/housing/tasks`，landing 同路径。`06` 截图 |
| AUTH-AFTER-REFRESH | PASS | 产品 API 增 page 后当前视图无 housing；浏览器 refresh 后出现 `/housing/dashboard`。`05` 截图 |
| PAM-005-PARK-SWITCH | **BLOCKED** | runner 把路由预先回落误当切园区完成；`07-park-switch` 截图的 selector 仍为 `JH`，未取得 Park B 完成态，禁止计 PASS |

观察项：PAM-005 页面出现 Ant Design `Alert.message` deprecation warning；精确过滤该 warning 后其余 console/runtime error gate 仍启用。该 warning 不影响本轮权限断言。

## 证据与 teardown

local-only 根：`/tmp/jinhu-pam-fix3-20260827-220612/`。

- Phase 0：`preflight/*.md`、`preflight/SHA256SUMS`
- 初始化：`migrate*.log`、`seed.log`、`baseline-*.log`、`bootstrap*.log`、`db/database-identity-after.txt`
- 预检：`curl-precheck.txt`、`login-precheck.json`、`screenshots/00-login-precheck.png`
- fixture：`ui-fixture*.log`、`api-fixture-results.json`、`network/ui-fixture-network.json`、`network/api-fixture-network.json`
- Case：`browser-*.log`、`browser-results.json`、`pam004-denied-state.json`、`network/browser-cases-network.json`
- DB：`db/touched-before.txt`、`db/touched-after-fixture.txt`、`db/residual-delete-attempt2.log`、`db/touched-after-cleanup.txt`
- 截图：`screenshots/*.png`、`screenshots/SHA256SUMS`

最终成功 runner 完成 UI logout → `/login` → `about:blank` → `Browser.close` → profile 删除。早期 `PAM-004` 失败路径发生在 runner 补强 teardown 之前，只执行 Browser.close/profile 删除，未留下完整 UI logout→about:blank 序列；这是独立 evidence blocker，不能用后续成功序列追补。

截图已人工检查关键 Case；未发现密码、JWT、Cookie、Authorization、连接串或个人敏感信息。包含秘密的临时 env/token/cookie/login response 文件已在 teardown 中安全删除。

## Residual 与资源清理

fixture 前冻结 13 张要求表，本轮精确 before 均为 0。fixture 后 / cleanup 后如下：

| 表 | fixture 后 | cleanup 后 |
| --- | ---: | ---: |
| `biz_park` | 1 | 0 |
| `sys_user` | 5 | 0 |
| `sys_role` | 3 | 0 |
| `rel_user_role` | 5 | 0 |
| `rel_user_park` | 10 | 0 |
| `rel_role_perm` | 15 | 0 |
| `rel_user_org` | 1 | 0 |
| `rel_role_data_scope` | 1 | 0 |
| `rel_role_field_perm` | 0 | 0 |
| `rel_role_field_policy` | 0 | 0 |
| `sys_auth_refresh_token` | 18 | 0 |
| `sys_login_log` | 18 | 0 |
| `sys_op_log` | 28 | 0 |

删除使用 fixture UUID、精确 username/code/park_id 和 RUN_ID remark；事务日志显示逐表 DELETE 数量。未使用宽谓词、TRUNCATE、trigger 绕过或 `session_replication_role`。

Park 创建的投影副作用可能涉及上述冻结清单以外的表；本轮没有在 fixture 前对全 schema 做逐表基线快照，因此不能声明“全部潜在 touched 表逐表 before/after”完全闭合。最终独占 compose 容器、volume、network 均为 0，`56537/3261/3262/9571` 监听为 0，专用 profile 为 0；该事实不能追补缺失的逐表 before 证据，故仍列 evidence blocker。

## 任务与遗留

- PAM-004、PAM-005 子任务：保持 `in_progress`，不归档。
- 权限审计父任务：保持未归档。
- §15 七组完整回归：未执行；六项矩阵尚有 1 项 BLOCKED，且 §15 本就超出该矩阵覆盖范围。
- 下一轮仅需用新 RUN_ID 修正 Park-switch wait：先记录切换前 park id，触发 select change 后等待 `/users/me.park_id == Park B`、selector value==Park B、Network `switch-context=2xx`，再断言 landing 属于 B Sidebar；失败路径从 runner 启动起即执行完整 teardown。还需在 fixture 前冻结产品 API 建 Park 的全部投影 touched 表。

未触碰 HR 系列、生产、主 Chrome或他人容器；未 force push。
