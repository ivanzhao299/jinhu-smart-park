# Phase 4 GitHub CI 记录（2026-08-06）

## PR

- Draft PR：`https://github.com/ivanzhao299/jinhu-smart-park/pull/223`。
- Head at creation：`6969bfa19858219c10895fe178b185e318cbb890`。
- Label：`run-release-smoke`。
- Auto-merge：未启用。

## 首轮 run

Run `31080366618` 因添加 label 触发同一 PR 的更高优先级 run，被 concurrency policy
取消；该 run 不计为产品或门禁失败。

## 第二轮 run 与根因

Run `31080385038` 在同一 head 上通过 install、lint、shared build 和 typecheck，在 API
unit tests 1030 项中出现唯一失败：

```text
ApiExceptionFilter passes the signed real HTTP no-existence-leak gate
git merge-base --is-ancestor 0152616f... <PR temporary merge commit>
```

测试在本地完整历史中通过，但 `actions/checkout@v5` 的默认浅克隆不包含签名 gate 的
base commit，因此 ancestry 命令返回非零。release-smoke 因 `needs: verify` 未运行。

独立 reviewer 确认该 ancestry 检查是签名 HTTP gate 的完整性合同，不能在浅克隆时跳过。
最小修复是在 verify job checkout 设置 `fetch-depth: 0`；不修改测试断言，也不扩大到没有
Git 历史依赖的 release-smoke job。

## 状态

- 修复浅克隆后的 run `31081245415`：verify 全部 PASS。
- 同一 run 的 release-smoke 在 Compose 解析阶段失败，未启动 PostgreSQL：production
  Compose 要求 `PARTY_DATA_ENCRYPTION_KEY`，但 release-smoke job env 未提供。诊断和
  teardown 因相同插值错误一起失败；没有容器或数据库被创建。
- 独立安全 reviewer 确认：只在一次性 synthetic CI 数据库使用公开的 40 字符
  `${{ github.sha }}`，满足 API production 长度检查且不提交真实秘密。该值可预测，明确
  不得用于生产；production Compose 和生产文档合同保持不变。
- 添加 CI-only key 后的 GitHub verify/release-smoke：待运行。
- PR 必须保持 Draft。
