# 设计

- 复用 `ds-page`、`ds-hero`、`ds-panel`、`ds-kpi-*`、`ds-command-*` 和 `ds-mobile-record-*`。
- 通过 HR 已共用的 `hr-workbench.module.css` 收敛模块视觉，不新建第二套按钮、色板或阴影。
- 工作台把“待办”和“功能入口”分离：待办只显示非零业务动作；全部清零时显示完成态。
- 快捷入口按业务域分组，桌面采用紧凑网格，手机采用两列触控入口。
- 审批业务流程不变，只优化 KPI、空态、表单展开和操作层级。

