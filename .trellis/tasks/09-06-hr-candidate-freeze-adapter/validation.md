# Validation

Implemented one pure adapter plus usable private IO command, reusing the existing bridge/generator. No production import activation, source extraction or business writes.

Independent check fixed untracked runtime dependencies being omitted from the code identity gate, and an unexpected output file race after receipt readback. Added real temporary Git and output-race regressions; synchronized the seven-section spec and operator documentation.

Root final rerun: pnpm test:e2e:yuzhou-production-import-real-artifact-bridge passed (existing bridge plus 38 new adapter/IO tests); node scripts/e2e/yuzhou-production-import-payload-generator-contract.mjs passed; git diff --check passed. Implementer/reviewer checked all five new JS files with node --check. Local pnpm lint/typecheck were attempted once by implementer but dependencies/ESLint/tsc unavailable. No installation or full build performed; remote CI still required.

Actual baseline release 29d40d9213d1297f6a58806612dec643faef7997: PR642 merged; main CI33989773671 succeeded, Release Smoke skipped by scope; Deploy Production33989773664 succeeded including protected accounts and confirmed Docker cleanup. These runs do not cover the new uncommitted adapter.

Read-only production inventory33990640586 succeeded on that baseline; artifact SHA88ab456a35fe9ca13e521d4441502a2e637c175998854a0f4c8fb1e34fc5e6d9, 6373 bytes. Complete sixteen-table validator passed; target identity/scope unchanged. Nineteen existing records: sixteen organizations and three contract types; all other fourteen tables zero. No production HR history import is claimed. No private paths or records are copied here.

Separate remaining acceptance: real source candidate use, authentic historical-to-current mapping lineage, external review/signature trust, AEAD execution context, real-scale memory, signed sealed plan and target activation, actual writer/readback, T4/binaries and full HR parity. Preparing READY is not completing any of those gates.

## Actual immutable candidate interface follow-up

After PR643 opened, root ran a supervised pure-adapter development check on existing immutable ee7 candidate/phase files; no production execution, source mutation or file output. Authenticated bytes read: 393636729. Peak RSS reported 452886528 bytes, below the 2 GiB supervisor termination threshold; Node heap bounded to 1536 MiB, 45-second timeout, core dumps disabled.

The check failed CANDIDATE_FREEZE_SHAPE_INVALID. Safe key-only inspection additionally found actual T2 materializer's resolutionEvidence (dictionaryPackageSha256, nullable changeDecisionsSha256, approvalClaimed:false), absent from the adapter's T2 envelope allowlist. Pure assembler-shaped tests had not covered the private materializer's augmented envelope. The T2 fix and actual nonempty materializer regression passed 58 focused tests, but the real-interface rerun still failed: safe module/line-only stack localization proved the immediate rejection occurs at target scope validation, before candidate parsing. The authenticated original target scope contains only tenantId and parkId, while the adapter required scopeSha256 as well. Therefore T2 was a genuine additional compatibility defect, not the proven cause of that first failure. Original private files remain unchanged. Follow-up accepts the actual two-field scope by computing its canonical scope hash, validates any supplied hash and rejects unknown fields; it does not relabel source or target evidence. Full real-input acceptance remains unproven until the subsequent bounded rerun passes.

Both envelope fixes passed the root's combined 60-test rerun. The next same-input supervised real check progressed into parsing but was terminated by MEMORY_GUARD at observed RSS 2147713024 bytes, just above the unchanged 2 GiB limit. Zero files or database writes. This is a resource failure, not successful interface acceptance. Review identified an unnecessary full Buffer copy in the parser; remove only that duplicate before one bounded retry. Do not increase the host resource limit to mask it.

Final real-input rerun after removing that duplicate passed in 8506 ms: 260828 records across all sixteen tables, 259630 insert candidates and 1198 quarantined candidates. Authenticated input bytes 393636729; process peak RSS 1943388160 bytes, below the unchanged 2 GiB supervisor limit. No files written and no database connection. The result remains REVIEW_HOLD with 1198 missing reviews and productionImport=HOLD. This validates the interface against immutable historical ee7 candidates, not a current production candidate or current repaired T2 materialization; do not confuse the old 1159 T2 quarantines with the separate current-semantic development result of eight.

Root final follow-up validation: combined T2 materializer and both freeze suites 62/62 PASS; existing real-artifact bridge and payload-generator contracts PASS; git diff --check PASS. Independent reviewer additionally ran the focused T3 candidate suite (75/75 combined), checked syntax and found no blocking issue in scope normalization, T2 provenance or direct byte-view parsing. SharedArrayBuffer is rejected to retain synchronous hash/decode integrity. The actual measured memory applies to the missing-review HOLD path, not the READY bridge/generator/output path. Remote CI for the final follow-up commit and production lineage/activation remain outstanding.
