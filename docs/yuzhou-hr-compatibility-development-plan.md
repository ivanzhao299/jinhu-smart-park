# Jinhu 企业级独立 HR 重写与玉舟兼容开发任务书

## 1. 目标与依据

本任务的产品目标是重写一套企业级、独立可复用、全功能的 Jinhu HR 产品，而不是只完成一次玉舟数据迁移，也不是在园区系统里增加若干 HR CRUD 页面。玉舟集团版 V10 的真实 SQL Server、Windows 客户端和集团 Web 旧页面是兼容事实输入；Jinhu Smart Park 当前 HR 首版代码是现代实现基线。目标是：

1. 保留玉舟已被企业使用的业务语义和历史数据可追溯性；
2. 用 Jinhu 当前 PostgreSQL、API、权限、Workflow Inbox 和响应式 Web 技术栈重构，不复制旧 Delphi/ASP.NET 代码；
3. 在完整产品能力之下提供旧 SQL Server 数据迁移、校验、回滚和并行核对能力，迁移交付不得替代功能重写；
4. 增加 HR 管理员、部门负责人、员工自助及后续财务/考勤专员等多用户端，落实 API 级原子权限和数据范围；
5. 把旧系统不安全或不可维护的实现替换为现代实现：明文密码不得迁移为可登录密码、动态工资表改为规范化明细、照片/附件改为文件对象引用。

事实依据：玉舟资料显示 162 张表；源码目录实物为 194 个过程文件、16 个函数文件、2 个触发器文件，旧分析报告中的“169 个存储过程”需在真实数据库恢复后按 catalog 口径核验；员工 2949 人、异动 6887 条、合同 802/357 条、五险一金 35008 条、工资约 4.5 万行、附件 1003 条。Jinhu 集成分支的 HR 基础迁移为 000230～000234，覆盖员工基础、目标/汇报、绩效/360、薪酬工资、审批。

## 2. 兼容原则

- **业务兼容优先，物理结构不兼容照实转换**：保留工号、组织编码、异动单号、合同号、工资账套、状态和历史日期语义；不照搬旧宽表和动态列。
- **历史永不删除**：玉舟 `person` 的在职、退休、离职、内退等统一进入 `hr_employee` 与 `hr_employment_event`；旧表名、旧主键和原始值进入迁移映射表。
- **状态机替代直接改字段**：就职/调动/离职/复职必须形成事件、前后快照、审批和审计；不能只覆盖员工当前部门或岗位。
- **权限原子化**：菜单权限、动作权限、字段权限、数据范围分离；前端隐藏不是授权依据，所有 API 重验 tenant/park/本人/负责人组织树。
- **金额不可变**：工资确认后禁止原地修改，使用更正批次；工资项、公式、税率和核算结果都要版本化。
- **证据分层**：代码、迁移、测试、推送、部署、health/ready、UAT 分开记录，不能用其中一项代替另一项。

### 2.1 HR 模块数据边界与可复用性

HR 当前采用“**共享 PostgreSQL、独立逻辑数据域**”而不是另起一个无租户、权限和审计能力的孤立数据库。所有 HR 业务、历史归档、生产导入操作和回执表均以 `hr_` 前缀归属 HR；模块导出/迁移时按目标 `tenant_id + park_id` 选择这些表。平台身份、组织、RBAC、审计和统一文件服务不复制进 HR 包，而是在目标范围重新绑定，这样不会把园区其他业务数据或登录身份随 HR 迁走。

`legacy_source_object`、`legacy_record_map`、`migration_batch`、`migration_batch_item`、`migration_error`、`migration_check`、`migration_rollback_point` 是共享迁移账本，不属于任何一个 HR 导出包的“全表复制”范围；玉舟 HR 仅携带 `source_system=yuzhou-v10` 且精确绑定本次 `batch_id` 的记录。所有 rollback 仍只能通过该批次的 active record map 删除或恢复其创建的目标行。照片和附件二进制不随模块数据包迁移，必须保持在独立 `T5_FILE` 切片并通过统一文件引用重新绑定。

该边界由 `scripts/hr-cutover/contracts/hr-module-boundary-v1.json` 固化，并由 `pnpm test:e2e:yuzhou-hr-module-boundary` 同时核验 DDL 锚点、共享账本选择和生产导入保持 `HOLD`。这保证模块可整体迁移/复用，同时不把“逻辑隔离”误报为已完成的物理拆库。

## 3. 旧系统到新系统的领域映射

| 玉舟领域 | 关键旧对象 | Jinhu 目标 | 处理方式 |
|---|---|---|---|
| 组织/岗位 | company、departmentcode、job、assignment | sys_org、hr_position | 保留原组织编码；按编码长度生成父子关系，建立旧编码映射 |
| 员工主档 | person、sfz、family、ticket；his 为独立动态历史 | hr_employee、hr_employee_profile、hr_employee_document + 扩展档案；his 进入无主兼容归档 | 宽表拆分；身份证、联系方式、证照、家庭分域并字段分级；his 未证明员工归属前不得写入履历 |
| 招聘人才 | accept | hr_candidate（新增） | 保留人才库与员工转化关系，不直接写入正式员工 |
| 入转调离 | readjust、readjustitem | hr_employment_event、hr_approval_request/action | 保留 JZ/DZ/LZ/FZ 单号和前后快照，旧状态映射为新状态机 |
| 合同 | compact、compact_c、compacttypecode | hr_contract、hr_contract_change（新增） | 主合同、续签/变更、试用期、附件、提醒和状态链 |
| 目标/汇报/绩效 | assessment*、asssession、asssour* | 已有 hr_goal*、hr_work_report、hr_performance* | 增加旧方案/项目/评分来源映射和历史快照 |
| 360 | asssour、asssourperson | 已有 hr_feedback* | 关系类型映射；匿名阈值必须重新计算，禁止泄露小样本 |
| 工资 | salary01..35、salaryitems、salaryequal、tax、salarycount | 已有 hr_compensation*、hr_payroll*、hr_payslip* + 新增公式版本模型 | 35 账套转配置表；动态列转工资项明细；公式先解析为受限 DSL，无法安全解析的进入人工复核 |
| 考勤 | timekeeptable、timekeeprecord、leave、overtime、attrecord | hr_attendance_*（新增） | 月历 31 列转日明细；导入批次、异常、请假/加班和工资联动可追溯 |
| 五险一金 | person_insure、insure_method | hr_insurance_policy、hr_employee_insurance、hr_insurance_period（新增） | 保留基数、单位/个人/补充额和政策版本；按月不可变快照 |
| 培训 | course、train、trainhis、jobtrain | hr_training_*（新增） | 课程、计划、参训、结果、费用和证据附件 |
| 奖惩 | bonuscode、bonusrecord、jch_1 | hr_reward_punishment_*（新增） | 类别、事件、金额/绩效联动、审批和审计 |
| 报表/查询 | templet、reports、reportsflt、FastReport | 查询方案、导出任务、审计报表（新增） | 先保证业务查询，再逐个复刻高价值报表；不迁移客户端报表引擎 |
| 权限/日志 | users、rights、logdata、person.loginid | sys_user、原子 permission、data scope、audit log | 密码只做身份核对/重置，不迁移旧明文密码；保留旧用户和日志关联 |

## 4. 多端、多用户与原子权限

### 用户端

1. HR 管理端：组织、人员、合同、异动、招聘、培训、绩效、薪酬、工资、考勤/社保运营；
2. 部门负责人端：组织树范围内人员、团队目标、汇报、绩效审核、待办；禁止薪资明细、证件原文和跨组织查询；
3. 员工自助端：本人档案可见字段、目标、汇报、绩效、自评、360 答卷、人事申请、已确认工资条；
4. 专项运营端：后续拆分考勤专员、薪酬专员、社保专员、招聘专员，按模块动作授权；
5. 移动端：员工和负责人优先，支持待办、汇报、审批、打卡/请假、工资条，不在移动端暴露高敏感批量管理。

### 原子权限模型

权限至少拆为：`module/page/action/field/data_scope` 五层。示例动作包括 `employee.read`、`employee.manage`、`employment.transition`、`payroll.calculate`、`payroll.review`、`payroll.confirm`、`payslip.self_read`。数据范围包括本人、直属团队、组织子树、指定单位、全园区。敏感字段（身份证、私人联系方式、银行卡、工资项、证件附件）单独字段权限和审计。

## 5. 分阶段开发路线

### 5.1 数据切片执行顺序（轻量优先）

历史数据导入不以开发难度或旧模块名称排序，而以“先建立可复用关系、先交付高收益轻量事实、最后处理最大且最敏感的工资明细”排序。受控执行契约为 `scripts/hr-cutover/contracts/lightweight-first-slice-order-v1.json`，顺序固定为：

1. `T0`：组织、岗位、员工主数据；
2. `T1`：入转调离等任职异动历史；
3. `T2`：合同及合同变更历史；
4. `T5_NONFILE`：不含照片和附件的员工档案、家庭关系、技能、证照；
5. `T3`：考勤、社保及现代请假/加班申请事实；
6. `T4`：工资历史与双轨核对，必须是最后一个数据写入切片。

`T5_NONFILE` 只使用稳定员工映射，禁止文件证据与自动身份归并；照片和附件为独立 `T5_FILE`，始终 `HOLD` 且不计入该顺序。完整 T0→T5 全域 A/B 契约用于最终整体复核，不改变上述分批落地顺序。任何生产历史导入仍需要一次性明确授权，当前固定为 `HOLD`。

### T0：基线、资料和迁移实验室

- 固化旧资料清单、SQL Server 备份校验、表/字段/字典/存储过程索引；
- 建立只读旧库容器和脱敏副本；
- 建立 `legacy_*` 映射、迁移批次、错误、校验和回滚记录模型；
- 产出旧→新字段映射表、编码映射表、状态映射表和数据质量报告。

### T1：员工与组织兼容闭环

- 扩展现有 `hr_employee`：旧工号、旧状态、扩展字段、家庭、履历、技能、证照、照片/附件；
- 组织编码树、岗位/职务/职称和编制映射；
- 就职、调动、离职、退休、复职单据及编号规则；
- HR/负责人/员工三端列表、详情、字段脱敏和组织树数据范围；
- 迁移 2949 名员工、138 个组织节点、6887 条异动，完成数量/关联/抽样复核。

### T2：合同、提醒、招聘、培训、奖惩

- 合同及续签/变更链，试用期/合同到期/生日/证照提醒；
- 招聘人才库与入职转化；
- 培训课程、计划、记录、费用；
- 奖惩记录及与绩效/工资的受控联动；
- 迁移合同 802/357、人才 117、附件 1003，并完成附件哈希和下载权限验证。

### T3：考勤与五险一金

- 考勤导入批次、班次、日明细、请假、加班、出差、异常处理；
- 月历到日明细兼容视图，保留旧符号语义；
- 社保政策版本、员工参保关系、月度基数与单位/个人/补充金额台账；
- 迁移并核对 35008 条保险记录和历史考勤；
- 先只读对账，再启用新系统计算，禁止未经批准直接替代正式薪资。

截至 2026-08-20，T3 的历史数据库兼容层已完成：两次只读抽取哈希一致；144 个月历转换为 4,383 个真实日期，旧符号原值保留，其中明确的“普通班次/晚上班”通过 `yuzhou-v1` 规则映射，`N/N1/N2` 保持待复核，不猜测出勤含义；12 套政策转换为 144 个纵向政策项；35,008 个员工社保快照中 34,787 个装载为不可变期间及 208,722 个六险种分项，216 个因 T0 员工未映射、5 个因年月缺失进入脱敏隔离。六险种金额逐类精确核对、员工摘要不变、同批次拒绝、精确回滚零残留和第二批次重装均已通过。请假、加班、出差、实际打卡流水仍属于后续业务/API 切片，不能把本次月历模板迁移描述为完整在线考勤。

### T4：工资兼容计算引擎

- 账套、工资项目、公式、条件、税率、计算顺序和关账模型；
- 旧 `salary01..35` 动态列转 `payroll_run/payslip/payslip_item`；
- 公式解析器采用白名单 DSL，记录原公式、解析状态、人工复核结果；
- 双轨计算对账：旧结果、新结果、差异原因和批准记录；
- 银行报盘、申报平台、财务总账仍列为后续集成，不在首轮默认开启。

### T5：历史迁移、并行运行与切换

- 全量迁移演练至少两次，增量窗口和停机切换方案；
- 关键表行数、金额汇总、员工状态、合同有效性、工资抽样和附件可读性校验；
- 旧系统只读封存，保留原始备份、映射、日志和回滚脚本；
- 通过 HR、部门负责人、员工三类 UAT 后再切换。

## 6. 每个切片的完成门槛

必须同时具备：需求与非目标、数据迁移/回滚、API/RBAC/DataScope/字段权限、状态机/幂等/并发/审计、桌面与移动页面、单元/集成/PostgreSQL/浏览器 E2E、lint/typecheck/build/security、迁移对账报告、最新 commit SHA、推送 SHA、部署 SHA、health/ready、三类角色 UAT 和已知限制。

## 7. 当前差距与首个执行任务

Jinhu 当前 HR 首版不是空白，已具备员工基础、目标/汇报、绩效/360、薪酬工资、审批和基础权限。但当前仍缺：

- 玉舟字段与历史编码兼容层；
- 合同、异动单号、招聘、培训、奖惩、考勤、社保完整领域；
- 工资账套/公式/税率迁移和双轨对账；
- 旧附件/照片迁移；
- 迁移批次、映射、错误、校验、回滚模型；
- 针对 HR 三端的完整原子字段权限和生产 UAT。

**首个开发任务：`HR-YZ-T0-MIGRATION-BASELINE`。** 先不修改生产数据，建立旧库只读访问、资料索引、目标映射草案、迁移审计模型和第一版数据质量报告。完成后再进入员工/组织兼容闭环。当前 Jinhu 工作树存在未跟踪的 `.trellis` 和 `docs/agent-memory`，且本地 `main` 与 `origin/main` 分叉 23/31；执行开发前必须先做提交谱系和 WIP 保护，不得 reset、覆盖或强推。

## 8. 已批准的 T0 执行方案（2026-08-19）

本机采用 Colima、Docker Compose、PostgreSQL 16 与 SQL Server 2022 Developer 组成双数据库实验室。Homebrew PostgreSQL 保持 5432；Jinhu 一次性目标库监听 `127.0.0.1:15432`；玉舟源库监听 `127.0.0.1:14333` 并以 `linux/amd64` 通过 Rosetta 运行。详细启动、验证、密钥和清理方法见 `docs/yuzhou-hr-migration-runbook.md`。

旧材料已完成全量只读扫描：共 220 个文件、218 个文本文件，7z 归档通过完整性测试且内含 217 个文件；清单使用相对路径和 SHA-256，不保存绝对路径或业务敏感值。2026-08-20 已接收并隔离恢复 SQL Server `.dbk` 备份，`VERIFYONLY` 通过，恢复库保持只读；catalog 实测为 162 表、169 过程、16 函数、2 触发器。下一步执行完整 catalog 导出、全量抽取和行级对账，不另写不可审计的临时导入脚本。

## 9. 现场运行态基线修正（2026-08-28）

只读现场遍历证明，玉舟遗留能力不能再按“客户端加一个员工自助 Web 壳”建模，而应保持三个独立证据面：

1. Windows 客户端负责传统组织、人事档案、入转调离、合同、工资、培训、奖惩、绩效、保险、考勤、招聘和系统设置；系统管理员页面按用户维护模块树权限，普通用户与系统管理员的可见能力不同。
2. 正在运行的集团 Web 版是独立 Classic ASP 站点，并连接独立企业 HR 数据库。现场数据库有 438 张表和 231 个 L1-L3 模块，其中 12 个一级域、100 个二级节点、119 个三级节点；186 个可导航入口全部能在部署源码中找到对应文件。部署源码共 6304 个文件，其中 4026 个 Classic ASP 页面。
3. 下载备份数据库中的 `sysTab`、`func`、`Web_GetSysTab` 和 `Web_GetUserRight` 是另一套较小 Web 菜单/权限证据，不能与正在运行的集团 Web 版合并计算，也不能据此推断两者功能相同。

集团 Web 版的权限模型已经包含模块级 `browse/query/add/edit/delete/state/print` 七类动作、角色模板、用户直授权和部门/部门范围数据权限。现场角色分配明显偏宽，迁移时只保留业务语义，不复制旧用户的叠加授权结果；新系统继续采用员工本人、负责人组织子树、HR 园区范围和专项运营角色的最小权限模型，并对敏感字段读取单独审计。

231 个集团 Web 模块按一级域分布为：组织 8、招聘 23、员工 22、考勤 29、薪酬福利 22、绩效 20、培训 31、事务 29、数据配置 4、决策 16、系统 9、个人办公 18。事务、数据配置和系统管理中的通用 OA 能力允许映射到 Smart Park 的工作流、平台管理或其他独立模块，但必须保留旧入口到新归属的可追溯映射，不能以“不属于 HR 页面”为由直接遗漏。

### 集团 Web 逐功能实施度门禁

`pnpm hr:migration:group-web:implementation-coverage` 对 231 个 L1-L3 节点逐项评分，禁止把“已有目标归属”直接解释成“功能已经复现”。评分只采用可在当前仓库重复验证的证据：

1. 目标归属与迁移责任明确：20 分；
2. 目标生产路由真实存在：20 分；
3. 存在对应 API 业务流：20 分；
4. 存在对应持久化模型或迁移：20 分；
5. 已完成该旧功能的字段、状态、计算和审批规则等价验证：10 分；
6. 已由对应真实角色完成生产或隔离 UAT：10 分。

只有 100 分才允许标记为 `implemented`；60-90 分为 `partial`；低于 60 分为 `mapped_only`。当前源码证据基线为 231 项全部纳管，190 项部分实现、41 项仅映射、0 项达到逐功能 100 分；其中 12 项已达到 90 分规则与字段等价门槛，平均 70.82 分，员工域平均 85.00 分。决策中心的聚合人员编制、员工统计、结构与流动查询已有独立页面/API/持久化证据：编制只统计当前授权园区的已启用岗位，在岗只计在职与试用人员，未设置编制与未分配岗位均单列，不以零值掩盖缺口；“员工信息汇总、人力资源预测、人力需求预测、内部供给预测、资本价值分析”现在共同归属决策中心，并以当前编制缺口、已归位/待归位供给、超编风险和员工档案规模提供不含个人信息的推演；它不猜测到岗时间、离职意向或未配置岗位需求。旧“人员招聘信息”精确归属到招聘工作区的需求、候选人管道与录用转预入职链；培训、绩效、考勤与薪酬福利信息则分别归属其独立工作区。上述入口仍缺旧报表逐字段规则等价与真实角色 UAT，因此只计入 partial，不得刷为完整实现。这个结果不是对当前 HR 模块整体可用性的否定，而是把旧系统的每个菜单能力拆开，明确指出尚缺真实角色 UAT。后续每一切片必须提升明确的旧功能 ID，不能只增加页面说明或笼统模块状态。

12 项 90 分功能的技术 UAT 采用 `scripts/hr-cutover/contracts/yuzhou-live-role-uat-task-card-v1.json`。任务卡固定 HR 制单与复核分离、部门负责人、员工本人三种业务角色，逐功能列出正向动作和越权负例，并要求桌面 1440×1000 与精确 390×844 两种视口同时验证。对应的 `scripts/hr-cutover/contracts/yuzhou-live-role-uat-api-matrix-v1.json` 进一步把 46 个正负向检查逐一冻结为角色、HTTP 方法、参数化 HR 路由、期望结果和不可省略的业务/越权断言；矩阵必须与任务卡逐项同序且哈希进入 A/B 证据，不能再用一个笼统的“接口已通”或手工 PASS 替代真实动作链。只有 Rehearsal A/B 在两套独立资源上执行同一任务卡和同一 API 矩阵、绑定完全相同的代码/源快照/映射合同三元组、逐项 API/审计/浏览器/敏感扫描全部通过且 P0/P1 为零，外部证据校验器才会把对应 ID 的 `liveRoleUat` 置为真。默认报告不读取外部证据，仍保持 90 分；传入 A/B 证据和预期三元组时还会要求证据代码 SHA 等于当前检出 SHA。机器技术 UAT、旧源语义复核和守恒结果统一进入 hash-addressed `machine_attestation`；不再等待 HR 人工签署，但生产历史导入在真实证据闭环前仍为 `HOLD`。

首个规则等价切片是旧功能 ID 313“工作日志”。现场源码确认旧字段为日志标题、工作日期、工作内容、问题和建议，并支持本人新增、修改、查询、删除与操作留痕。新系统补充独立的标题与问题建议字段，保留日报/周报/月报、主管审核、退回重提和目标关联等新能力；旧版物理删除被替换为仅草稿或退回稿可撤销的软删除，动作链永久留痕。该功能已由 80 分提升到 90 分，待迁移与真实员工/主管双角色 UAT 后才能标记为 100 分。

第二个规则等价切片是旧功能 ID 34“员工入职”。对集团 Web 的 `employee/register` 页面、校验脚本、审批保存类和只读数据库结构交叉核对后，确认旧流程不是直接新增员工，而是“录用人员转预入职 → 填写入职申请 → 提交审批 → 非申请人审核 → 审批同意后确认入职”的分阶段事务。新系统新增独立入职申请台账，保留申请名称、申请日期、计划入职日、试用月数、数字考勤卡号、候选人来源与备注；员工、候选人、组织和岗位全部按租户/园区外键校验，员工与考勤卡在未取消申请中唯一。确认入职只有在申请已批准且员工仍为待入职状态时才可执行，并在同一数据库事务内更新员工状态、入职日期、试用期截止日、考勤卡号，生成玉舟兼容的 `JZ` 任职事件编号和不可篡改动作链。旧版回收站删除被现代化为可审计取消，申请人不能审批本人申请。该功能在完成迁移门禁和 HR 申请/审核/确认三角色生产 UAT 前仍不标记 100 分，生产历史导入继续 HOLD。

第三个规则等价切片是旧功能 ID 35“员工基本信息”。集团 Web 真实 `basicinfo` 新增、编辑、详情、列表与保存类共同确认了身份人口、教育语言、联系方式、职务资格四组字段。新系统不照搬旧版明文证件存储：证件号采用加密正文、脱敏投影和规范化指纹三件套，并在租户/园区范围内防重；出生日期、民族、籍贯、政治面貌、身高体重、婚姻健康、户籍、学历专业学位、语言能力、毕业学校、职务职级、员工类别、技术职称等字段进入受保护员工档案。普通档案读取只得到脱敏投影，具备敏感档案管理权限的 HR 才能读取和修改完整字段，读取字段组进入独立审计。旧源文件只保存相对路径与内容哈希，仓库不记录旧员工值；生产历史导入继续 HOLD。

第四个规则等价切片是旧功能 ID 36“员工转正”。旧系统实际由转正申请表、多人转正清单、审批结果与流程结束双门槛、最终批量变更组成；新系统据此实现草稿、提交、退回、复提、审批、取消、确认状态机，冻结提交时员工清单，并用制单复核分离、幂等写、行锁和单事务完成批量确认。最终副作用只把仍处于试用期的员工更新为在职、写入转正日期并追加不可变任职事件，不沿用旧系统语义混乱的离职标志字段，也不写工资或绩效事实。旧源仍只记录相对路径、内容哈希和规则摘要，生产历史导入继续 HOLD。

第五个规则等价切片是旧功能 ID 37“员工合同”。集团 Web 的新增、编辑、详情、查询、保存、附件和操作记录页面共同确认合同编号唯一、合同类型、员工、期限、起止/签订/转正/生效日期、岗位、工种、部门快照、试用月数、试用与转正工资、备注和合同附件。新系统补齐这些字段，合同草稿允许受控编辑，生效、取消、续签、变更和终止均追加不可更新、不可删除的动作流水；旧版物理删除被替换为可审计取消。合同工资写入单独要求薪酬管理权限，读取单独要求薪酬读取权限；合同附件使用合同 ID 作为受保护业务引用，并沿用本人、负责人组织子树和 HR 园区范围。旧源码证据仅保存相对路径、哈希和规则摘要；在完成 HR 管理员、部门负责人、员工本人和薪酬专员四角色 UAT 前保持 90 分，生产历史导入继续 HOLD。

第六个规则等价切片是旧功能 ID 39“岗位变更”。本切片明确区分两个旧入口：集团 Web 版负责岗位变更申请、审批结果、流程结束与审批后手工生效；桌面客户端以 `readjust` 台账维护 `DZyyyymmxxxx` 编号及入职、调动、离职、复职等统一人事异动历史。新系统保留两者共同业务语义，但不复制两套数据：岗位变更申请采用草稿、提交、退回、复提、批准、取消、生效状态机，审批人与制单人分离；只有已批准且到达生效日的申请才能在单事务中更新员工部门/岗位并追加同编号任职事件。审批后若员工当前归属已变化则失败关闭，要求退回重取快照；旧工资字段与变更标志不在岗位变更中直接写工资事实，而进入独立薪酬变更兼容切片。部门负责人只可办理其组织树，HR 具有园区范围权限，动作证据只追加。旧源证据仅保留相对路径、哈希、字段和规则摘要，生产历史导入继续 HOLD。

第七个规则等价切片覆盖旧功能 ID 42-47“员工离职”六项三级功能。集团 Web 版的离职申请、面谈、调查、交接、工资结算和关闭档案已分别核对页面源码与只读数据库结构；桌面客户端仍作为离职任职事件的最终台账来源，两端证据不能互相替代。新系统以一张离职申请聚合草稿、提交、退回、批准、取消和生效状态，审批人与制单人分离；批准后必须逐项完成或经授权豁免面谈、调查、工作交接和工资结算，最后关闭人事档案，方可在计划日期到达后原子更新员工状态并写入同编号 `LZyyyymmxxxx` 任职事件。跨月修改自动重编编号，审批后员工归属或状态漂移时失败关闭，所有动作只追加且所有写接口具备幂等语义。旧系统锁定登录账号的做法不与档案关闭耦合，改由独立身份访问控制处理；已生效后的入离职清单继续作为后置运营流程，而不是重复生效。六项功能从 80 分提升至 90 分，尚需 HR、部门负责人、员工本人、面谈、调查、交接、薪酬、档案和生效角色的生产 UAT 才能达到 100 分；历史导入继续 HOLD。

仅映射项仍主要集中在事务管理、数据配置、决策中心和个人办公中笼统指向 `/hr` 的入口。已将系统管理、用户、角色、权限、数据范围、字典与审计日志的玉舟入口绑定到现有 `/system/*` 页面及其 API/持久化证据；旧“提醒设置”则精确绑定到现有合同/试用期提醒工作区，具备权限分层、幂等运行、审计和收件箱闭环。它不等同于旧规则完全复现：生日、父母生日、证照到期和工龄等通用提醒策略仍是明确缺口。剩余 `/admin` 与 `/workflow` 引用不是实际生产页面路径：流程定义及其余缺失能力必须新建真实业务功能或映射到已验证的精确页面，不能继续依赖概念路径。

可执行事实合同见 `scripts/hr-cutover/contracts/legacy-group-web-live-runtime-v1.json`。该合同只保存模块数量、不可逆哈希、权限结构、目标归属和缺口，不保存账号、密码、员工值、现场地址、源码或截图。生产历史导入继续保持 `HOLD`；L4 运行态证据必须与字段/规则清单、金额守恒、A/B、CAS、回滚和 residual=0 一起形成机器复核凭证，不能单独触发导入。

## 10. 双源数据迁移基线（2026-08-28）

客户端恢复库与集团 Web 现场库必须作为两个独立来源进入迁移，不得把集团 Web 当作客户端数据库的一组页面。客户端来源保留传统人事及工资历史，已审计 162 表、2364 字段、212 条数据库规则、46 个帮助主题和 915 条授权记录；集团 Web 来源保留企业人事运营，已审计 438 表、5449 字段、215 个非空表和 320406 行，并在专用 `VIEW DEFINITION` 只读账号下确认 768 个视图、340 个过程、9 个函数和 79 个触发器。438/5449 的旧系统兼容口径包含 SQL Server 2005 标记为 `is_ms_shipped` 的 1 张零行兼容表；排除该表会得到 437/5442，但不能据此误报业务数据减少。两者都必须在目标记录上保留 `source_id`、来源业务键哈希和抽取批次，不允许覆盖为一个不明来源的“玉舟数据”。

集团 Web 员工主档为 548 行，其中按旧删除/离职标志得到的在职候选为 134 行。与客户端员工按工号或证件规范化哈希核对后，19 行已有匹配，115 行只存在于集团 Web；全量 548 行中按工号匹配 313 行、按证件哈希匹配 308 行、两者并集匹配 316 行，仍有 232 行无工号或证件匹配。姓名不参与自动合并。匹配优先级固定为证件规范化哈希、工号、人工复核；冲突进入隔离，不自动创建在线员工、账号、审批、消息、工资或绩效记录。

可执行合同为 `scripts/hr-cutover/contracts/legacy-dual-source-reconciliation-v1.json`，验证入口为 `pnpm test:e2e:yuzhou-legacy-dual-source-reconciliation`。合同固定两套 catalog、集团 Web 逐表 `COUNT_BIG(*)` 精确前缀汇总、关键业务表计数、员工核对方程和不可逆证据哈希；旧 `sys.partitions` 汇总只能作为快速估算，不得作为迁移守恒。任何行数、哈希、身份策略或生产开关漂移均失败关闭。下一阶段先在隔离库生成 115 行脱敏人工核对队列并完成字段级来源优先级，再执行双源 load→verify→rollback→residual=0→reload。生产历史导入继续 `HOLD`，普通部署不得调用历史 loader。

## 11. 字段、API 与前端原子覆盖基线（2026-09-04）

真实结构清单的机器校验结果固定为 162 表、2,364 字段、194 个过程、16 个函数、2 个触发器和 46 个帮助页面。现有 reviewed core mapping 只覆盖员工档案、任职异动、合同三个核心域的 12 表/260 字段：38 个字段有规范化目标字段和实现证据，220 个字段按原始列名进入授权详情归档，旧密码与照片二进制 2 个字段不进入通用字段投影。其余 150 表/2,104 字段和 205 条数据库规则仍需逐项确认目标域、API、页面、权限和测试，不能因为存在通用归档表就记为完整兼容。

宽旧表采用“列表摘要、详情按需”的读取方式。`000290_hr_legacy_archive_full_field_projection.sql` 将已纳入 T5A 的非二进制原字段写入敏感权限保护的 `legacyFields` 详情投影；普通列表不返回该宽投影，详情 API 在 required audit 成功后才返回，前端按字段逐项展开。因此已评审 220 个归档字段具备可查询、可分页定位记录、详情全字段展示的技术链，但这不替代规范化业务模型，也不为未评审的 2,104 个字段提供完成信用。

执行 `pnpm hr:migration:legacy-modern-surface:audit -- --inventory <绝对路径>` 可输出不含业务值的汇总回执。只有每个旧字段都处于 `precise_mapped`、`authorized_archive_detail` 或明确安全排除之一，每条旧规则均绑定新状态机/API 正负向测试，每个入口通过真实角色桌面与 390px UAT，才能报告完整复现。

## 12. 真实旧库关系与全字段映射纠偏（2026-09-04）

兼容开发顺序固定调整为“旧库关系分析 → 现代模型对照 → 字段拆分/转换 → API/前端承载 → 数据导入”，迁移回滚能力不得替代字段与功能等价。`legacy-relational-model.mjs` 从真实 `schema_tables.sql`、162 表原子清单和 212 个例程源码生成只含结构的关系模型；当前核验为 109 个声明主键、52 个声明外键、104 个隐式业务键候选，212 个例程中有 64 张表被活动 SQL 直接引用。扫描器排除注释中的废弃语句，并要求表出现在 `FROM/JOIN/UPDATE/INTO/ALTER` 或表限定列位置，避免把 `leave` 等关键字误算为依赖。隐式候选必须结合代码表、过程依赖和业务说明复核，不得按同名字段自动写入。

`legacy-modern-table-domain-map-v1.json` 将 162 张表恰好一次归入组织岗位、员工档案、招聘、任职、合同、工资、考勤、社保、绩效、培训、奖惩、计件、报表、协同记录、权限审计、自定义配置或技术残留，并声明现代目标表及宽表拆分方法。`legacy-modern-field-ledger.mjs` 再为 2,364 个字段生成逐项账本，分别记录提取状态、关系、拆分规则、已实现目标、计划目标和功能状态；密码字段拒绝迁移，`image/binary` 字段改走文件对象，不允许进入普通 JSON 投影。

当前候选代码按实际抽取与写入语句核验后的基线为：

- 2,364/2,364 字段已被账本分类，清单覆盖率 100%，但这只表示“没有字段从计划中消失”；
- 66 张表、1,526 个字段已有字段级抽取证据；1,055 个字段声明了规范化现代目标，但其中只有经过可执行存储/API/页面/测试证据校验的字段才能计入“已验证功能覆盖率”，声明目标不得直接得分；
- 302 个字段当前仅能通过受权历史详情查看，168 个字段已提取但尚未接入目标业务字段；
- 828 个字段尚缺字段级提取，其中包括组织/岗位漏列、社保账号和参保标志，以及尚未进入抽取链的绩效、考勤业务、计件、报表和自定义配置表；
- 38 张表所在域仍需要新增或补齐现代目标结构。旧 `13.75/100` 聚合分数不能再作为字段兼容事实，也不能把本节的“清单覆盖 100%”冒充功能完成 100%。

长宽字段固定按以下方式分解：组织编码 3/6/9/12 位层级生成显式 `parent_id`；人员主档按核心任职、受保护档案、薪酬、社保、银行、自定义值和文件拆分；`date1..date31` 转逐日考勤；六险宽字段转期间与险种明细；35 张工资表的动态 `S/U/Q` 列转带类型的工资项事实。下一阶段按高收益和依赖关系清零：员工主档剩余字段、自定义字段目标结构、组织/岗位漏列、社保漏列、绩效/考勤业务表、其他业务表，最后处理纯配置和技术残留。

结构关系核验命令：

```bash
pnpm hr:migration:legacy-relations:audit -- --inventory <原子清单绝对路径> --schema <schema_tables.sql绝对路径> --routine-directory <例程目录绝对路径> --json
```

字段账本核验命令：

```bash
pnpm hr:migration:legacy-field-ledger:audit -- --inventory <原子清单绝对路径> --relations <关系模型绝对路径> --json
```

## 13. 存储过程、函数与触发器逻辑复现基线（2026-09-04）

数据库例程是旧系统功能规格源，不再只按“引用过哪些表”统计。`legacy-routine-logic-ledger.mjs` 对恢复库对应的 194 个存储过程、16 个函数和 2 个触发器逐对象读取活动 SQL，生成 212 行一对一逻辑账。每行固定记录：参数名与 SQL 类型、读表、写表、外部/生成表、被调用例程、字段连接条件、条件分支、聚合、舍入、空值默认、日期运算、组织前缀范围、动态 SQL、游标、事务信号、主/次业务域、现代目标表、服务、API、页面、复现状态和风险等级。账本只保存结构与源码哈希，不保存员工值、工资值、凭据或附件内容。

当前真实源码核验结果是：

- 212/212 个例程均已建立原子逻辑映射，清单覆盖率 100%；该数字只表示没有例程遗漏，不表示功能已经等价；
- 44 个例程具有活动写表行为，57 个包含动态 SQL，119 个跨越两个或更多业务域；写例程数由新版解析器纠正：它识别不带 `INTO` 的旧式 `INSERT` 与 `MERGE`，同时排除注释和动态片段造成的伪目标；这些对象必须优先做输入、公式、状态副作用和守恒测试；
- 176 个业务例程已经找到现代领域表/API/页面，但仍是 `partial_domain_surface_rule_parity_pending`，不能因现代页面存在就记为完成；
- 2 个自定义字段函数仍缺现代目标能力；`getdef/getdefs` 负责动态扩展字段标签与查询投影，必须与 `person.def*` 拆分一起实现；
- 32 个 SQL Server 设计器/遗留工具例程进入“确认无业务依赖后退役”，不能未经调用链确认直接删除；
- 2 个考勤项目触发器的活动体均只有 `SELECT 1`，旧动态加列代码已经注释；新系统以纵向考勤项目/日事实替代，不复刻运行时改表；
- 源码还引用 37 个备份 DDL 未包含的外部、临时或生成对象，包含 `tn1..tn6` 统计报表中间表、系统目录及过程内工作表；这些引用已单独标记，不允许当作普通业务主表静默忽略。

复现顺序按依赖而不是按文件名：先组织/员工/字典查找函数，再入转调离与合同状态逻辑，再考勤和社保计算，再绩效/培训/奖惩，最后工资跨域取数与统计报表。特别是 `u_fromunit` 及其历史版本同时读取组织、员工、考勤、社保、绩效和奖惩并动态写入 `salary01..35`，属于最高风险工资输入编排；必须拆成版本化输入适配器，禁止把原动态 SQL 直接搬进 PostgreSQL 或 API。

例程逻辑账核验命令：

```bash
pnpm hr:migration:legacy-routine-logic:audit -- --inventory <原子清单绝对路径> --relational-model <关系模型绝对路径> --table-map <表域映射绝对路径> --capability-map <例程能力映射绝对路径> --routine-directory <例程目录绝对路径> --repository-root <仓库绝对路径> --json
```

每个业务例程只有在其旧输入 fixture、现代服务输出、状态副作用、金额/数量守恒、权限负例和桌面/390px 页面结果全部一致后，才能从 pending 改为 equivalent。历史变体（`_bak/_bak2/_bak3/_old/2003`）必须归入同一 canonical family 比较差异；不能只验证最新同名版本，也不能把“已迁历史数据”当作“已复现业务逻辑”。

## 14. 全模型重写目标、零遗漏规则与里程碑（2026-09-04）

本项目按大型专业 HR 产品的技术栈重写执行，不再按园区附属 CRUD 模块估算。生产迁移不是独立终点，而是全模型兼容完成后的交付动作。事实源分为三层，三层均不可互相替代：DDL 定义系统可表达的结构，存储过程/函数/触发器源码定义计算和状态逻辑，当前备份实例只定义本次需要迁移的实际值。因此空表、全空列、没有被当前数据触发的分支、当前没有调用证据的例程仍须进入映射账和测试计划；不得以“本次数据为空”为理由删除功能。

兼容完成度拆成五个独立分母，全部达到 100% 才能称为完整复现：

1. 结构兼容：客户端 162 张表/2,364 个字段与集团 Web 438 张表/5,449 个字段分别建立不可混淆的原子账；每项均有现代目标、受权历史目标、安全排除或经调用链证明的技术退役结论；
2. 逻辑兼容：客户端 194 个存储过程/16 个函数/2 个触发器与集团 Web 340 个存储过程/9 个函数/79 个触发器分别具有业务域、输入、输出、读写、公式、分支、副作用和现代实现结论；恢复库 catalog 与源码归档的对象差异单独核销；
3. 交互兼容：C/S 帮助和实机页面，以及集团 Web 231 个菜单节点、186 个可导航入口和 4,026 个 ASP 页面，均按字段、按钮条件、操作顺序、角色权限和报表布局绑定到现代页面/API；
4. 数据兼容：宽表拆分、空值、类型、关系、金额和记录数守恒均有自动验证，历史查询能够从新前端完整到达；
5. 运行兼容：目标提交、部署、生产前备份、一次导入、分域核对、权限负例、桌面/390px UAT 和逆序回滚均通过。

M0-M5 是当前开发责任边界；每条线只领取自己的原子合同、实现和验收证据，不跨线改写事实或虚增完成度：

| 里程碑 | 独占责任 | 出口与不得冒充的信用 |
|---|---|---|
| M0 全景建模 | 冻结双源表、字段、关系、例程、菜单、页面和空对象分母，维护 hash-bound 原子账与 gap/receipt | 出口是稳定身份和处置状态；结构盘点、静态引用和清单 100% 都不等于功能等价 |
| M1 人员主链 | 组织岗位、员工、字典、自定义字段、入转调离、合同及其关系、页面和 API | 出口是源关系到现代存储、API、权限、页面和正负向测试的最小闭环；归档可查不等于业务能力等价 |
| M2 专业业务域 | 招聘、考勤休假、社保福利、绩效、培训和奖惩 | 出口是对应例程族的输入、分支、状态、副作用、权限和守恒证据；有目标表或任务卡不等于已运行 |
| M3 薪酬与报表 | 工资输入适配、工资项事实、期间计算、税额/舍入、统计中间表和报表查询 | 出口是 PostgreSQL numeric 计算、跨域输入、金额守恒和报表口径等价；历史工资已装载不等于工资引擎等价 |
| M4 页面复现 | 与 M1-M3 并行遍历 Windows C/S 帮助/实机页面和集团 Web，冻结字段、动作、条件、角色、状态和报表布局，并完成现代桌面与 390px 页面 | 出口是真实角色下旧页面与现代路由/API 的逐入口运行证据；源码存在、静态路由命中或“任务已准备”均不得记运行等价 |
| M5 迁移闭环 | 只消费已冻结映射，生成抽取、转换、写入、隔离、回读、守恒、A/B 和逆序回滚零残留证据 | 出口是固定 C/S/M 的隔离环境闭环；演练、回执或 loader 存在均不得记为生产导入 |

生产切换位于 M0-M5 之后，是独立的发布与一次性授权门禁，不属于当前并发开发责任线。当前任务没有生产写入或生产导入授权。

工期采用滚动燃尽，不再用文件大小、Agent 数量或已生成合同数量估算。全功能重写的长期区间见第 16 节；每完成一个可运行里程碑，按机器账本的真实增量、返工率和旧源访问稳定性重新估算。

## 15. 滚动并发实施方案（2026-09-04）

M0-M5 不按六条完全独立的直线同时写代码，因为 M1-M5 都必须消费同一份 M0 原子身份、关系和规则合同；脱离合同并发会制造第二套映射。平台并发上限按 4 个活跃槽位执行：`1 个主任务 + 最多 3 个子 Agent`。主任务持续承担事实校准、共享入口集成、冲突审查和总体验收；三个子 Agent 只领取文件所有权互斥、可独立验证的 M0-M5 最小切片。任一子 Agent 完成后，主任务先复核其合同、差异和测试，再立即从真实剩余账本补入下一个最小切片，使子 Agent 槽位在有安全工作时保持最多 3 个，而不是等一整批同时结束。

滚动派发遵循以下约束：

- 每个子 Agent 固定一个 M0-M5 责任前缀及对应合同/测试，不修改其他线文件，不回退或覆盖并行 WIP；有跨线依赖时只提交 gap/receipt 和最小接线建议；
- 每一波只合入能够形成“源身份 → 映射 → 现代存储/API/页面 → 正负向测试 → 迁移/回滚”的最小业务族；页面遍历随业务族同步，不积压到最后；
- 真实 SQL Server 的只读 catalog、DDL、例程源码及值域/计数回执，和 Windows 客户端/集团 Web 旧页面的真实字段、动作、条件、角色与状态，是关闭兼容项的关键输入；静态推断无法唯一确定时建立明确 gap/receipt，不猜测数据或业务语义；
- 任何回执和提交均不得包含凭据、个人数据、工资明细、附件内容或生产连接信息；旧源只读，实验目标受限，生产保持禁写。

共享入口实行单线集成：`apps/api/src/modules/hr/entities/hr.entities.ts`、`apps/api/src/modules/hr/hr.service.ts`、`apps/api/src/modules/hr/hr.controller.ts`、`apps/api/src/modules/hr/hr.module.ts` 和 `apps/web/lib/hr-api.ts` 只由主任务按已复核切片接线并做跨层类型核对。子 Agent 不直接并发修改这些文件；数据库迁移序号也由主任务统一协调，避免重复编号和实体/列名漂移。共享入口通过编译并不自动提升任何旧功能信用，信用仍来自对应 M0-M5 原子证据。

阶段出口采用机器可验收条件：M0 要求所有双源原子项有稳定身份和处置状态；M1-M3 要求每个 canonical routine family 的输入、输出、分支、副作用、权限和守恒证据齐全；M4 要求真实角色矩阵下旧页面与现代桌面/390px 的字段、动作和报表可达；M5 要求隔离目标上的写入后回读、数量/金额守恒、幂等重放和逆序回滚零残留。只有这些出口和独立生产门禁全部通过，才可另行申请固定提交、目标、源快照和窗口的一次性生产导入授权；本文不声称已经生产导入。

## 16. 当前保守燃尽基线与下一批次（2026-09-05）

`pnpm hr:migration:legacy-compatibility:progress` 生成不可加总的六维进度，而不是继续使用一个会把清单盘点、代码存在和真实等价混在一起的总分。当前候选代码机器核验为：

- 客户端字段已验证目标映射 `182/2364 = 7.7%`：核心域 `38/260`、组织岗位 `25/50`、工资字段 `21/32`、自定义配置 `36/36`、员工技能 `4/5`、奖惩 `2/16`、培训历史 `5/23`、保险政策 `51/51`、绩效考核定义 `0/11`，跨切片重复为 0；保险政策字段已区分百分数费率与固定加项，但对应月度计算例程仍须单独完成行为等价；
- 客户端例程结构建账 `212/212`，完整行为等价为 `5/212 = 2.36%`；已闭合 1 个请假查询、2 个考勤结构钩子、1 个职称字典查询和 1 个家庭成员查询，结构盘点不得获得功能等价信用；
- 客户端权限源已守恒捕获 `915/915` 条授权边，源派生能力 `76/93 = 81.72%` 已映射；授权边只用于守恒且功能信用仍为 0，17 个能力仍待目标评审，禁止复制旧用户名或用户叠加授权来凑数；
- 组织岗位关系 `7/8`，工资动态例程 `0/6`；
- 自定义字段现代界面检查 `21/21`，但旧交互等价 `0/6`；集团 Web 已准备可执行任务 `7/186 = 3.76%`，运行等价仍为 `0/186`；
- 当前候选生产证据 `0/8`，`productionImport=HOLD`。历史隔离演练、旧部署回执或通用归档查询均不计入当前生产证据，也不表示已经生产导入。

这些数字说明前几天完成的主要是受控迁移底座、全量分母、保护性存储和少数核心闭环，并不等于旧系统全功能已重写。近期里程碑按阻断最少顺序推进：

1. 清零组织岗位剩余 11 个字段和 1 个关系，但人员身份绑定项必须等待明确映射，不能姓名猜测；
2. 完成 `u_errandrecords` 的旧来源存储、组织名称和申报天数展示，再以一次只读 catalog/计数回执关闭时区与内连接数据语义；
3. 逐个解决 6 个工资动态例程；`u_inputbasepay` 首先只需 catalog 证明 `person._base` 与 `person.basepay` 的真实关系，不需要读取工资明细；
4. 每关闭一个例程族，同步完成 API 权限负例、桌面与 390px 页面、迁移回读和逆序回滚，不积压到最后；
5. 以一小族真实旧页面为单位，把集团 Web 的静态定义、SQL/过程引用、现代路由/API 和真实角色运行证据串成首批完整链；未取得旧页面运行观察时继续保持 `runtime 0/186`，不得用任务卡或源码命中代替；
6. M0-M4 的可确定核心族达到出口后，才进入 M5 固定 C/S/M 的隔离 A/B、回读、备份恢复和逆序零残留；在生产证据仍为 `0/8` 时继续 `productionImport=HOLD`。

长期工期按“企业级独立可复用全功能 HR 重写 + 双源兼容闭环”估算，而不是只估迁移脚本：

- 乐观区间为 **14–20 周**。前提是真实 SQL Server 只读实例、完整例程源码及 Windows/集团 Web 旧页面能持续访问，需求边界和业务决策及时稳定；平台始终可维持 3 子 Agent + 主任务的互斥滚动并发；共享入口单线集成顺畅；隔离 PostgreSQL、API、浏览器和角色环境可重复使用；没有新增大范围数据清洗、外部系统或隐藏报表范围。
- 稳妥区间为 **26–40 周**。该区间覆盖旧源或旧页面访问间歇、隐藏例程/角色分支/报表被逐步发现、历史数据质量与身份关系需要隔离复核、跨域薪酬计算返工，以及安全、性能、移动端、权限负例、A/B、恢复回滚和多角色 UAT 的完整硬化。

两个区间都以需求冻结后的连续投入为前提，并须在每个近期里程碑后按实际燃尽速度、返工率、未决 gap 和可用测试环境重新估算。它们不是上线承诺、生产导入日期或缩短证据门槛的理由；生产导入在独立硬门禁和一次性授权完成前始终为 `HOLD`。

上述目标、M0-M5 出口、真实四槽滚动并发边界、预测假设和重新估算触发器已经固化为机器合同 `scripts/hr-cutover/contracts/hr-enterprise-rewrite-roadmap-v2.json`，由 `pnpm hr:migration:enterprise-roadmap` 校验。当前关键路径按持续可访问的真实源和旧页面计算为：第 1-2 周冻结 M0 全景账；第 2-7 周完成 M1 人员主链；第 3-10 周并行完成 M2 专业域；第 3-12 周完成 M3 薪酬计算与报表；M4 从第 2 周伴随各业务族推进并在乐观情形第 14 周形成双旧端逐入口运行证据；M5 在 M0-M4 达到出口后用第 15-20 周完成全域演练、回读、恢复和生产切换。若源访问、页面语义、历史质量或业务复核出现持续阻断，则按稳妥区间第 26-40 周收口。所有生产门禁通过后，单次历史导入及分域核对预计另需 1-3 天；这 1-3 天不能替代前面的功能等价工作。
